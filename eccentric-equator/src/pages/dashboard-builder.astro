---
const mode = import.meta.env.PUBLIC_MODE || 'public';
const isPublic = mode === 'public';

if (isPublic) {
  return Astro.redirect('/404');
}

import Layout from '../layouts/Layout.astro';
import { DashboardBuilder } from '../components/DashboardBuilder';
---

<Layout title="Strategy Dashboard Builder — Hackfluency" description="Design quarterly cybersecurity strategy dashboards with connected nodes.">
  <DashboardBuilder client:only="react" />
</Layout>

<style is:global>
  body {
    overflow: hidden;
  }
</style>
