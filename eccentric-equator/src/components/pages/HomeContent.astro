---
import NavAuthButtons from '../Auth/NavAuthButtons.astro';
import LanguageSelector from '../LanguageSelector.astro';
import { getTranslations } from '../../i18n/utils';
import type { Language } from '../../i18n/translations';
import { subscribeEmail } from '../../lib/subscription';

interface Props {
  locale?: Language;
}
const { locale = 'es' } = Astro.props;
const { t } = getTranslations(locale);
const base = import.meta.env.BASE_URL;

// SVG icons for services - elegant, minimal line art
const serviceIcons = {
  shield: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/><circle cx="12" cy="12" r="3" opacity="0.4"/><path d="M9 9l3 3 3-3" opacity="0.6"/></svg>`,
  cpu: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="4" width="16" height="16" rx="2"/><circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M2 12h2M20 12h2"/><circle cx="12" cy="12" r="1" opacity="0.3"/><path d="M8 8l2 2M14 14l2 2M14 8l-2 2M10 14l-2 2" opacity="0.3"/></svg>`,
  target: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="6"/><circle cx="12" cy="12" r="2"/><path d="M12 2v3M12 19v3M2 12h3M19 12h3" opacity="0.4"/></svg>`,
  brain: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M9.5 2A2.5 2.5 0 0 1 12 4.5v15a2.5 2.5 0 0 1-4.96-.44 2.5 2.5 0 0 1-2.96-3.08 3 3 0 0 1-.34-5.58 2.5 2.5 0 0 1 1.32-4.24 2.5 2.5 0 0 1 4.44-1.66z"/><path d="M14.5 2A2.5 2.5 0 0 0 12 4.5v15a2.5 2.5 0 0 0 4.96-.44 2.5 2.5 0 0 0 2.96-3.08 3 3 0 0 0 .34-5.58 2.5 2.5 0 0 0-1.32-4.24 2.5 2.5 0 0 0-4.44-1.66z"/><circle cx="12" cy="8" r="1.5" opacity="0.3"/><circle cx="12" cy="12" r="1.5" opacity="0.3"/><circle cx="12" cy="16" r="1.5" opacity="0.3"/></svg>`,
  users: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/><path d="M9 9l4 4" opacity="0.3"/><path d="M13 9l-4 4" opacity="0.3"/></svg>`,
  flask: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M9 3h6"/><path d="M10 3v5l-3.5 7A2 2 0 0 0 8.3 18h7.4a2 2 0 0 0 1.8-3L14 8V3"/><path d="M6.5 14h11"/><circle cx="12" cy="10" r="1" opacity="0.3"/></svg>`,
  eye: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/><path d="M8 12l3 3 5-5" opacity="0.4"/><path d="M4 12h2M18 12h2" opacity="0.3"/></svg>`,
  lock: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/><path d="M12 15v3" opacity="0.6"/><circle cx="12" cy="16.5" r="0.5" opacity="0.4"/></svg>`,
  compass: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76"/><circle cx="12" cy="12" r="2" opacity="0.3"/></svg>`,
  activity: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/><circle cx="12" cy="8" r="1" opacity="0.3"/><circle cx="12" cy="16" r="1" opacity="0.3"/></svg>`
};

// SVG icons for values - refined symbols
const valueIcons = {
  excellence: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/><circle cx="12" cy="10" r="1.5" opacity="0.3"/></svg>`,
  evolution: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2v4m0 12v4M4.93 4.93l2.83 2.83m8.48 8.48l2.83 2.83M2 12h4m12 0h4M4.93 19.07l2.83-2.83m8.48-8.48l2.83-2.83"/><circle cx="12" cy="12" r="3"/><path d="M10 12l2 2 4-4" opacity="0.4"/></svg>`,
  elegance: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/><circle cx="12" cy="7" r="1" opacity="0.3"/></svg>`,
  integrity: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20.42 4.58a5.4 5.4 0 0 0-7.65 0l-.77.78-.77-.78a5.4 5.4 0 0 0-7.65 0C1.46 6.7 1.33 10.28 4 13l8 8 8-8c2.67-2.72 2.54-6.3.42-8.42z"/><circle cx="12" cy="13" r="1.5" opacity="0.3"/></svg>`,
  understanding: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/><path d="M7 9l4 4" opacity="0.3"/><path d="M9 7l6 6" opacity="0.3"/></svg>`,
  depth: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M2 12h20"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/><circle cx="12" cy="12" r="2" opacity="0.2"/></svg>`,
  gear: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M12 1v2M12 21v2M1 12h2M21 12h2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"/><circle cx="12" cy="12" r="7" opacity="0.2"/></svg>`,
};

const serviceColor: Record<string, string> = {
  strategicAdvisory: 'var(--hf-accent)',
  securityEngineering: 'var(--hf-accent-analysis)',
  offensiveSecurity: 'var(--hf-accent-secondary)',
  aiSystems: 'var(--hf-accent-privacy)',
  training: 'var(--hf-accent-human)',
  research: 'var(--hf-accent-philosophy)',
  dataPrivacy: 'var(--hf-accent-privacy)',
  intelligenceAnalysis: 'var(--hf-accent-analysis)',
};

const services: { icon: string; key: string; category: string; link?: string; linkText?: string }[] = [
  { icon: serviceIcons.shield, key: 'strategicAdvisory', category: 'human' },
  { icon: serviceIcons.cpu, key: 'securityEngineering', category: 'digital' },
  { icon: serviceIcons.target, key: 'offensiveSecurity', category: 'digital' },
  { icon: serviceIcons.brain, key: 'aiSystems', category: 'digital' },
  { icon: serviceIcons.users, key: 'training', category: 'human' },
  { icon: serviceIcons.flask, key: 'research', category: 'analytical', link: '/research/tooling', linkText: 'services.explore' },
  { icon: serviceIcons.lock, key: 'dataPrivacy', category: 'digital' },
  { icon: serviceIcons.eye, key: 'intelligenceAnalysis', category: 'analytical' },
];

const values = [
  { titleKey: 'values.excellence.title', descKey: 'values.excellence.desc', icon: valueIcons.excellence },
  { titleKey: 'values.evolution.title', descKey: 'values.evolution.desc', icon: valueIcons.evolution },
  { titleKey: 'values.elegance.title', descKey: 'values.elegance.desc', icon: valueIcons.elegance },
  { titleKey: 'values.integrity.title', descKey: 'values.integrity.desc', icon: valueIcons.integrity },
  { titleKey: 'values.understanding.title', descKey: 'values.understanding.desc', icon: valueIcons.understanding },
  { titleKey: 'values.depth.title', descKey: 'values.depth.desc', icon: valueIcons.depth },
];
---

<!-- Mobile Menu Toggle Script -->
<script>
  document.addEventListener('astro:page-load', () => {
    const menuBtn = document.querySelector('.mobile-menu-btn');
    const mobileMenu = document.getElementById('mobile-menu');
    const body = document.body;
    
    if (menuBtn && mobileMenu) {
      menuBtn.addEventListener('click', (e) => {
        e.preventDefault();
        const isOpen = mobileMenu.classList.toggle('active');
        menuBtn.classList.toggle('active');
        menuBtn.setAttribute('aria-expanded', isOpen.toString());
        menuBtn.setAttribute('aria-label', isOpen ? 'Close mobile menu' : 'Open mobile menu');
        body.style.overflow = isOpen ? 'hidden' : '';
        // Fix language switcher contrast on dark overlay
        if (isOpen) {
          var langSwitcher = document.querySelector('.lang-switcher') as HTMLElement | null;
          if (langSwitcher && !langSwitcher.dataset.hfFixed) {
            langSwitcher.dataset.hfFixed = '1';
            langSwitcher.style.setProperty('background', 'rgba(255,255,255,0.08)', 'important');
            langSwitcher.style.setProperty('border-color', 'rgba(255,255,255,0.2)', 'important');
            langSwitcher.style.setProperty('color', '#e0e0e0', 'important');
          }
        }
      });
      
      // Close menu when clicking on a link
      const mobileLinks = mobileMenu.querySelectorAll('a');
      mobileLinks.forEach(link => {
        link.addEventListener('click', () => {
          mobileMenu.classList.remove('active');
          menuBtn.classList.remove('active');
          menuBtn.setAttribute('aria-expanded', 'false');
          menuBtn.setAttribute('aria-label', 'Open mobile menu');
          body.style.overflow = '';
        });
      });
      
      // Close menu when clicking outside
      mobileMenu.addEventListener('click', (e) => {
        if (e.target === mobileMenu) {
          mobileMenu.classList.remove('active');
          menuBtn.classList.remove('active');
          menuBtn.setAttribute('aria-expanded', 'false');
          menuBtn.setAttribute('aria-label', 'Open mobile menu');
          body.style.overflow = '';
        }
      });
    }

    // Navigation scroll effect
    const nav = document.querySelector('.nav');
    window.addEventListener('scroll', () => {
      if (window.scrollY > 50) {
        nav?.classList.add('scrolled');
      } else {
        nav?.classList.remove('scrolled');
      }
    });
  });

  // Chrono-spec tooltip toggle (touch devices only)
  (function() {
    if (!window.matchMedia('(hover: none)').matches) return;

    document.addEventListener('click', function(e) {
      var s = (e.target as HTMLElement | null)?.closest('.chrono-spec');
      var u = (e.target as HTMLElement | null)?.closest('.chrono-unit');

      // Tap on tooltip → close
      if (s) {
        u && u.classList.remove('active');
        return;
      }
      // Tap on gear → toggle
      if (u) {
        var all = document.querySelectorAll('.chrono-unit');
        for (var i = 0; i < all.length; i++) {
          if (all[i] !== u) all[i].classList.remove('active');
        }
        u.classList.toggle('active');
        return;
      }
      // Tap outside → close all
      var all = document.querySelectorAll('.chrono-unit');
      for (var i = 0; i < all.length; i++) all[i].classList.remove('active');
    });
  })();
</script>

<!-- Newsletter Subscription Script -->
<script>
  import { subscribeEmail } from '../../lib/subscription';

  // ===== CONFIGURATION =====
  const MAX_EMAIL_LENGTH = 254;
  const ALLOWED_EMAIL_CHARS = /^[a-zA-Z0-9@._-]+$/;

  // ===== PROBLEM FORM HANDLER =====
  const problemForm = document.getElementById("problem-form");
  const problemMessage = document.getElementById("problem-form-message");

  if (problemForm && problemMessage) {
    problemForm.addEventListener("submit", async function (e) {
      e.preventDefault();

      const name = (document.getElementById("problem-name") as HTMLInputElement)?.value.trim() || "";
      const email = (document.getElementById("problem-email") as HTMLInputElement)?.value.trim();
      const area = (document.getElementById("problem-area") as HTMLSelectElement)?.value;
      const contact = (document.getElementById("problem-contact") as HTMLSelectElement)?.value;
      const description = (document.getElementById("problem-description") as HTMLTextAreaElement)?.value.trim();

      const getMsg = (key: string) => problemForm?.getAttribute(`data-msg-${key}`) || '';

      if (!email || !description) {
        problemMessage.textContent = getMsg('required');
        problemMessage.className = "form-message error";
        return;
      }
      if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
        problemMessage.textContent = getMsg('email-invalid');
        problemMessage.className = "form-message error";
        return;
      }

      problemMessage.textContent = getMsg('sending');
      problemMessage.className = "form-message info";

      const locale = window.location.pathname.startsWith('/en') ? 'en' : 'es';

      try {
        const BOT_TOKEN = import.meta.env.PUBLIC_TELEGRAM_BOT_TOKEN;
        const CHAT_ID = import.meta.env.PUBLIC_TELEGRAM_CHAT_ID;
        const esc = (t: string) => (t || '').replace(/[<>&]/g, (c) => ({'<':'&lt;','>':'&gt;','&':'&amp;'}[c] || c));

        const areaLabels: Record<string,string> = { security: 'Seguridad', architecture: 'Arquitectura', behavior: 'Comportamiento organizacional', intelligence: 'Inteligencia estratégica', other: 'Otro' };
        const contactLabels: Record<string,string> = { email: 'Email', meeting: 'Reunión breve', depends: 'Depende' };

        const text = '<b>🔬 Nuevo problema recibido</b>\n<b>Nombre:</b> ' + esc(name || '(sin nombre)') + '\n<b>Email:</b> ' + esc(email) + '\n<b>Área:</b> ' + esc(areaLabels[area] || area || '') + '\n<b>Contacto:</b> ' + esc(contactLabels[contact] || contact || '') + '\n<b>Descripción:</b>\n' + esc(description) + '\n<b>Fecha:</b> ' + new Date().toISOString();

        if (BOT_TOKEN && CHAT_ID) {
          const res = await fetch('https://api.telegram.org/bot' + BOT_TOKEN + '/sendMessage', {
            method: 'POST', headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ chat_id: Number(CHAT_ID), text, parse_mode: 'HTML' }),
          });
          if (res.ok) {
            problemMessage.textContent = getMsg('success');
            problemMessage.className = "form-message success";
            (problemForm as HTMLFormElement).reset();
            return;
          }
        }
        problemMessage.textContent = getMsg('error');
        problemMessage.className = "form-message error";
      } catch (err) {
        problemMessage.textContent = getMsg('network');
        problemMessage.className = "form-message error";
      }
    });
  }

  // ===== DOM ELEMENTS =====
  const emailInput = document.getElementById("email-input") as HTMLInputElement | null;
  const message = document.getElementById("form-message") as HTMLElement | null;
  const subscribeForm = document.getElementById("subscribe-form") as HTMLFormElement | null;

  // ===== VALIDATION FUNCTIONS =====

  function sanitizeEmail(email: string): string {
    email = email.trim();
    email = email.replace(/[^a-zA-Z0-9@._-]/g, '');
    return email;
  }

  function validateEmailFormat(email: string): boolean {
    const emailRegex = /^[a-zA-Z0-9][a-zA-Z0-9._-]*@[a-zA-Z0-9][a-zA-Z0-9.-]*\.[a-zA-Z]{2,}$/;
    return emailRegex.test(email);
  }

  function validateEmailLength(email: string): boolean {
    return email.length <= MAX_EMAIL_LENGTH;
  }

  function validateEmail(email: string): { valid: boolean; error?: string } {
    if (!email) {
      return { valid: false, error: subscribeForm?.getAttribute('data-msg-required') ?? '' };
    }

    if (!validateEmailLength(email)) {
      return { valid: false, error: subscribeForm?.getAttribute('data-msg-too-long') ?? '' };
    }

    if (!ALLOWED_EMAIL_CHARS.test(email)) {
      return { valid: false, error: subscribeForm?.getAttribute('data-msg-invalid-chars') ?? '' };
    }

    if (!validateEmailFormat(email)) {
      return { valid: false, error: subscribeForm?.getAttribute('data-msg-invalid-format') ?? '' };
    }

    return { valid: true };
  }

  // ===== REAL-TIME VALIDATION =====
  if (emailInput && message) {
    emailInput.addEventListener("input", function(e) {
      const target = e.target as HTMLInputElement;
      const email = target.value;
      const validation = validateEmail(email);

      if (email.length === 0) {
        message.textContent = "";
        message.className = "form-message";
        return;
      }

      if (!validation.valid) {
        message.textContent = validation.error ?? "";
        message.className = "form-message error";
        return;
      }

      message.textContent = subscribeForm?.getAttribute('data-msg-valid') ?? '';
      message.className = "form-message success";
    });

    emailInput.addEventListener("blur", function(e) {
      const target = e.target as HTMLInputElement;
      const email = target.value;
      if (email.length === 0) {
        message.textContent = "";
        message.className = "form-message";
      }
    });

    subscribeForm?.addEventListener("submit", async function (e) {
        e.preventDefault();

        const email = sanitizeEmail(emailInput.value);
        const validation = validateEmail(email);

        if (!validation.valid) {
          message.textContent = validation.error ?? "";
          message.className = "form-message error";
          return;
        }

        message.className = "form-message info";
        message.textContent = subscribeForm?.getAttribute('data-msg-subscribing') ?? '';

        try {
          const result = await subscribeEmail(email);

          if (result.success) {
            message.textContent = subscribeForm?.getAttribute('data-msg-success') ?? '';
            emailInput.value = "";
            message.className = "form-message success";
            return;
          }
          if (result.message === 'Already subscribed.') {
            message.textContent = subscribeForm?.getAttribute('data-msg-already') ?? '';
            message.className = "form-message warning";
            return;
          }
          message.textContent = subscribeForm?.getAttribute('data-msg-error') ?? result.message;

        } catch (error) {
          if (import.meta.env.DEV) console.error(error);
          message.textContent = subscribeForm?.getAttribute('data-msg-network') ?? '';
          message.className = "form-message error";
        }
      });
  }
</script>

<!-- Navigation -->
<nav class="nav">
  <div class="nav-container">
    <a href={base} class="logo" aria-label={locale === 'es' ? 'Hackfluency - Ir al inicio' : 'Hackfluency - Go to homepage'}>
      <img src={`${base}HFNeon.webp`} alt="Hackfluency logo" width="56" class="logo-img" />
      <span class="logo-text"><span class="logo-hack">Hack</span><span class="logo-fluency">fluency</span></span>
    </a>
    <div class="nav-links">
      <a href="#lineas">{t('nav.services')}</a>
      <a href="#grupo">{t('nav.about')}</a>
      <a href="#values">{t('nav.approach')}</a>
      <a href={locale === 'es' ? '/research' : '/en/research'} class="nav-research">{t('nav.research')}</a>
    </div>
    <div class="nav-actions">
      <LanguageSelector locale={locale} />
      <NavAuthButtons locale={locale} />
    </div>
    <button class="mobile-menu-btn" aria-label={t('nav.menuOpen')} aria-expanded="false" aria-controls="mobile-menu">
      <span></span>
      <span></span>
      <span></span>
    </button>
  </div>
  
  <!-- Mobile Menu Overlay -->
  <div class="mobile-menu-overlay" id="mobile-menu">
    <div class="mobile-menu-content">
      <nav class="mobile-nav-links">
        <a href="#lineas" class="mobile-nav-link">{t('nav.services')}</a>
        <a href="#grupo" class="mobile-nav-link">{t('nav.about')}</a>
        <a href="#values" class="mobile-nav-link">{t('nav.approach')}</a>
          <a href={locale === 'es' ? '/research' : '/en/research'} class="mobile-nav-link mobile-nav-link--research">{t('nav.research')} ↗</a>
      </nav>
      <div class="mobile-nav-actions">
        <LanguageSelector locale={locale} />
        <NavAuthButtons locale={locale} mobile={true} />
      </div>
    </div>
  </div>
</nav>

<!-- Hero Section -->
<main id="main-content" role="main">
<section class="hero">
  <div class="hero-bg">
    <div class="hero-glow"></div>
    <div class="hero-grid"></div>
    <div class="hero-particles"></div>
  </div>
  <div class="paper-hero container">
    <!-- Paper header -->
    <div class="paper-head">
      <div class="paper-meta">
        <span class="paper-ref">HF-RPT-2026-003</span>
        <span class="paper-sep">·</span>
        <span class="paper-date">{locale === 'es' ? 'Julio 2026' : 'July 2026'}</span>
        <span class="paper-sep">·</span>
        <span class="paper-tag">Convergencia Normativa</span>
      </div>
      <div class="paper-authors">
        <a href="https://hackfluency.com/research" class="paper-org">{locale === 'es' ? 'Hackfluency — Observatorio' : 'Hackfluency — Observatory'}</a>
      </div>
    </div>

    <!-- Title area -->
    <div class="paper-title-area">
      <h1 class="paper-title">Convergencia Normativa</h1>
      <p class="paper-chart-lead">{locale === 'es' ? 'CHILE 2026 · ¿Qué controles te cubren de cada ley?' : 'CHILE 2026 · Which controls cover you under each law?'}</p>
      <p class="paper-desc">{locale === 'es' ? 'Cada flecha es un artículo legal exacto. 4 leyes aplicables · 17 controles · 1 hoja de ruta unificada. Descubre cuáles te alcanzan.' : 'Each arrow is an exact legal article. 4 applicable laws · 17 controls · 1 unified roadmap. Find out which ones apply to you.'}</p>
    </div>

    <!-- Content: Sankey + Conclusions -->
    <div class="paper-grid">
      <div class="paper-sankey">
        <iframe src="/research/articles/convergencia-normativa/sankey" class="hf-sankey-iframe" loading="lazy" title="Diagrama de convergencia normativa"></iframe>
        <div class="sankey-mobile-preview">
<svg viewBox="0 0 400 152" xmlns="http://www.w3.org/2000/svg" style="width:100%;height:auto;display:block;border-radius:12px;border:1px solid var(--hf-border,#222);background:white;">
            <defs>
              <style>
                .mh{font-family:Segoe UI,Inter,Arial,sans-serif;font-size:10px;font-weight:700;text-anchor:middle;fill:#fff}
                .ml{font-family:Segoe UI,Inter,Arial,sans-serif;font-size:9px;font-weight:600;fill:#1A1A18}
                .mc{font-family:Segoe UI,Inter,Arial,sans-serif;font-size:8px;fill:#666}
                .mt{font-family:Segoe UI,Inter,Arial,sans-serif;font-size:8px;font-weight:700;fill:#fff;text-anchor:middle}
              </style>
            </defs>

            <!-- ===== Tramo 1: LEYES (izquierda) ===== -->
            <text x="70" y="20" class="ml" text-anchor="middle" font-size="8" letter-spacing="0.08em" fill="#444">{locale === 'es' ? 'LEYES' : 'LAWS'}</text>
            <rect x="20" y="28" width="100" height="22" rx="11" fill="#DC3545"/>
            <text x="70" y="42" class="mt">21.663</text>
            <rect x="20" y="54" width="100" height="22" rx="11" fill="#5B6ABF"/>
            <text x="70" y="68" class="mt">21.719</text>
            <rect x="20" y="80" width="100" height="22" rx="11" fill="#6F42C1"/>
            <text x="70" y="94" class="mt">21.595</text>
            <rect x="20" y="106" width="100" height="22" rx="11" fill="#E67E22"/>
            <text x="70" y="120" class="mt">NCG</text>

            <!-- ===== Flechas de flujo Ley -> Estándar (paralelas, ancladas) ===== -->
            <path d="M 122 40 L 142 40" stroke="#DC3545" stroke-width="2.5" opacity="0.75"/>
            <path d="M 122 64 L 142 64" stroke="#5B6ABF" stroke-width="2.5" opacity="0.75"/>
            <path d="M 122 87 L 142 87" stroke="#6F42C1" stroke-width="2.5" opacity="0.75"/>
            <path d="M 122 112 L 142 112" stroke="#E67E22" stroke-width="2.5" opacity="0.75"/>

            <!-- ===== Tramo 2: ESTÁNDARES (centro) — azul como en sankey desktop, nombres reales ===== -->
            <text x="187" y="20" class="ml" text-anchor="middle" font-size="8" letter-spacing="0.08em" fill="#444">{locale === 'es' ? 'ESTÁNDARES' : 'STANDARDS'}</text>
            <rect x="142" y="28" width="90" height="20" rx="10" fill="#7FA8DC"/>
            <text x="187" y="41" class="mc" text-anchor="middle" font-size="7.5" font-weight="700" fill="#063A6E">ISO 27001 · SGSI</text>
            <rect x="142" y="52" width="90" height="20" rx="10" fill="#7FA8DC"/>
            <text x="187" y="65" class="mc" text-anchor="middle" font-size="7.5" font-weight="700" fill="#063A6E">NCG 538 · CMF</text>
            <rect x="142" y="76" width="90" height="20" rx="10" fill="#7FA8DC"/>
            <text x="187" y="89" class="mc" text-anchor="middle" font-size="7.5" font-weight="700" fill="#063A6E">BOD 26-04 · Vuln.</text>
            <rect x="142" y="100" width="90" height="20" rx="10" fill="#7FA8DC"/>
            <text x="187" y="113" class="mc" text-anchor="middle" font-size="7.5" font-weight="700" fill="#063A6E">{locale === 'es' ? 'Reporte & Notif.' : 'Reporting & Notif.'}</text>
            <rect x="142" y="124" width="90" height="20" rx="10" fill="#7FA8DC"/>
            <text x="187" y="137" class="mc" text-anchor="middle" font-size="7.5" font-weight="700" fill="#063A6E">{locale === 'es' ? 'Modelo Prev. 20.393' : 'Prevention Model 20.393'}</text>

            <!-- ===== Conexiones Estándar -> Controles (abanico diagonal convergente) ===== -->
            <path d="M 232 38 L 292 56" stroke="#9AA0A6" stroke-width="2" stroke-dasharray="5 4" opacity="0.8"/>
            <path d="M 232 62 L 292 68" stroke="#9AA0A6" stroke-width="2" stroke-dasharray="5 4" opacity="0.8"/>
            <path d="M 232 86 L 292 80" stroke="#9AA0A6" stroke-width="2" stroke-dasharray="5 4" opacity="0.8"/>
            <path d="M 232 110 L 292 92" stroke="#9AA0A6" stroke-width="2" stroke-dasharray="5 4" opacity="0.8"/>
            <path d="M 232 134 L 292 104" stroke="#9AA0A6" stroke-width="2" stroke-dasharray="5 4" opacity="0.8"/>

            <!-- ===== Tramo 3: CONTROLES (derecha) ===== -->
            <text x="336" y="44" class="ml" text-anchor="middle" font-size="8" letter-spacing="0.08em" fill="#444">{locale === 'es' ? 'CONTROLES' : 'CONTROLS'}</text>
            <rect x="292" y="54" width="88" height="50" rx="10" fill="#1B4332"/>
            <text x="336" y="80" class="mh" font-size="22">17</text>
            <text x="336" y="96" class="mh" font-size="8">{locale === 'es' ? 'controles' : 'controls'}</text>
          </svg>
<a href="/research/articles/convergencia-normativa" class="sankey-mobile-cta">{locale === 'es' ? 'Descargar informe completo (PDF) ↓' : 'Download full report (PDF) ↓'}</a>
        </div>
      </div>
      <div class="paper-findings">
        <div class="findings-header">
          <span class="findings-icon">🔎</span>
          <span class="findings-label">{locale === 'es' ? '¿Qué te aplica a ti?' : 'What applies to you?'}</span>
        </div>
        <div class="diag-grid">
          <div class="diag-chip">
            <span class="diag-num">4</span>
            <span class="diag-caption">{locale === 'es' ? 'leyes que pueden alcanzarte' : 'laws that may apply to you'}</span>
          </div>
          <div class="diag-chip">
            <span class="diag-num">17</span>
            <span class="diag-caption">{locale === 'es' ? 'controles para revisar' : 'controls to review'}</span>
          </div>
          <div class="diag-chip">
            <span class="diag-num">2<span class="diag-unit">min</span></span>
            <span class="diag-caption">{locale === 'es' ? 'de diagnóstico, sin tecnicismos' : 'quick assessment, no jargon'}</span>
          </div>
        </div>
        <div class="diag-actions">
          <a href="/research/autodiagnostico/" class="paper-cta paper-cta--primary">
            <span class="cta-line cta-line--main">
              {locale === 'es' ? 'Diagnóstico Guiado Gratis →' : 'Guided Assessment Free →'}
            </span>
            <span class="cta-line cta-line--sub">
              {locale === 'es' ? 'Obtén tu reporte' : 'Get your report'}
            </span>
          </a>
          <a href="/research/articles/convergencia-normativa" class="paper-cta">
            {locale === 'es' ? 'Explorar el análisis interactivo →' : 'Explore the interactive analysis →'}
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Ticker de Últimas Indagaciones -->
<section class="ticker-wrap"
  data-items={JSON.stringify([
    { tag: t('indagaciones.item1.tag'), title: t('indagaciones.item1.titulo'), fecha: t('indagaciones.item1.fecha'), color: '#F59E0B', recent: true },
    { tag: t('indagaciones.item2.tag'), title: t('indagaciones.item2.titulo'), fecha: t('indagaciones.item2.fecha'), color: '#6C4F9E', recent: false },
    { tag: t('indagaciones.item5.tag'), title: t('indagaciones.item5.titulo'), fecha: t('indagaciones.item5.fecha'), color: '#00D26A', recent: false },
    { tag: t('indagaciones.item3.tag'), title: t('indagaciones.item3.titulo'), fecha: t('indagaciones.item3.fecha'), color: '#3B82F6', recent: false },
    { tag: t('indagaciones.item6.tag'), title: t('indagaciones.item6.titulo'), fecha: t('indagaciones.item6.fecha'), color: '#9C5E4D', recent: false },
    { tag: t('indagaciones.item7.tag'), title: t('indagaciones.item7.titulo'), fecha: t('indagaciones.item7.fecha'), color: '#9C5E4D', recent: false },
    { tag: t('indagaciones.item4.tag'), title: t('indagaciones.item4.titulo'), fecha: t('indagaciones.item4.fecha'), color: '#3B82F6', recent: false },
  ])}>
  <div class="ticker-header">
    <span class="ticker-header-badge">{t('indagaciones.tag')}</span>
    <span class="ticker-header-live">
      <span class="ticker-live-dot"></span>
      {locale === 'es' ? '7 entregas · en vivo' : '7 deliveries · live'}
    </span>
  </div>
  <div class="ticker-track" id="hf-ticker">
    <div class="ticker-inner" id="hf-ticker-inner"></div>
  </div>
</section>

<script is:inline data-astro-rerun>
(function() {
  var wrap = document.querySelector('.ticker-wrap');
  if (!wrap) return;
  var raw = wrap.getAttribute('data-items');
  var items;
  try { items = JSON.parse(raw); } catch(e) { return; }
  if (!items || !items.length) return;

  // Double the items for seamless loop
  var doubled = items.concat(items);
  var inner = document.getElementById('hf-ticker-inner');
  if (!inner) return;

  inner.innerHTML = doubled.map(function(item, idx) {
    var color = item.color || 'var(--hf-accent)';
    var isRecent = item.recent;
    var pulseDur = isRecent ? '1.5s' : '3s';
    var animName = isRecent ? 'tickerPulseRecent' : 'tickerPulseOld';
    var dotSize = isRecent ? '14px' : '10px';
    var glowIntensity = isRecent ? '0 0 14px ' + color : '0 0 6px ' + color;
    var tagBg = color + '20';
    var tagBorder = color + '40';
    return '<div class="ticker-item' + (isRecent ? ' ticker-item--recent' : '') + '" style="border-color:' + color + '60;' + (isRecent ? 'border-width:1.5px;' : '') + '">' +
      '<span class="ticker-dot" style="background:' + color + ';box-shadow:' + glowIntensity + ';animation:' + animName + ' ' + pulseDur + ' ease-in-out infinite;width:' + dotSize + ';height:' + dotSize + ';"></span>' +
      '<span class="ticker-meta">' +
        '<span class="ticker-tag" style="background:' + tagBg + ';border-color:' + tagBorder + ';color:' + color + ';">' + item.tag + '</span>' +
        '<span class="ticker-fecha">' + item.fecha + '</span>' +
      '</span>' +
      '<span class="ticker-title">' + item.title + '</span>' +
    '</div>';
  }).join('');

  // Calculate animation duration based on total width
  var totalWidth = 0;
  var children = inner.children;
  for (var i = 0; i < children.length; i++) {
    totalWidth += children[i].offsetWidth;
  }
  var duration = Math.max(20, totalWidth / 80); // ~80px per second
  inner.style.setProperty('--ticker-duration', duration + 's');
  inner.style.animationDuration = duration + 's';
  inner.style.animationPlayState = 'running';

  // Pause on hover
  var track = document.getElementById('hf-ticker');
  if (track) {
    track.addEventListener('mouseenter', function() { inner.style.animationPlayState = 'paused'; });
    track.addEventListener('mouseleave', function() { inner.style.animationPlayState = 'running'; });
  }

  // Accessibility: reduce motion
  var mq = window.matchMedia('(prefers-reduced-motion: reduce)');
  if (mq.matches) { inner.style.animation = 'none'; inner.style.transform = 'none'; }
})();
</script>

<style is:global>
  .ticker-wrap {
    background: var(--hf-bg-secondary, #111);
    border-top: 1px solid var(--hf-accent-border, rgba(0,210,106,0.15));
    border-bottom: 1px solid var(--hf-accent-border, rgba(0,210,106,0.15));
    padding: 12px 0;
    overflow: hidden;
    position: relative;
  }
  html[data-theme-mode="light"] .ticker-wrap {
    background: color-mix(in srgb, var(--hf-accent) 5%, var(--hf-bg-secondary));
    border-color: color-mix(in srgb, var(--hf-accent) 20%, transparent);
  }
  .ticker-wrap::before, .ticker-wrap::after {
    content: '';
    position: absolute;
    top: 0; bottom: 0;
    width: 60px;
    z-index: 2;
    pointer-events: none;
  }
  .ticker-wrap::before { left: 0; background: linear-gradient(90deg, var(--hf-bg-secondary, #111) 0%, transparent 100%); }
  .ticker-wrap::after { right: 0; background: linear-gradient(270deg, var(--hf-bg-secondary, #111) 0%, transparent 100%); }
  .ticker-header {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 16px;
    padding: 0 24px 10px;
  }
  .ticker-header-badge {
    font-size: 0.6rem;
    font-weight: 700;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--hf-accent, #00D26A);
    border: 1px solid var(--hf-accent-border, rgba(0,210,106,0.3));
    padding: 3px 12px;
    border-radius: 100px;
  }
  .ticker-header-live {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 0.6rem;
    font-weight: 600;
    color: var(--hf-text-dim, #666);
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }
  .ticker-live-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--hf-accent, #00D26A);
    animation: tickerLivePulse 2s ease-in-out infinite;
  }
  @keyframes tickerLivePulse {
    0%, 100% { opacity: 1; transform: scale(1); box-shadow: 0 0 6px var(--hf-accent, #00D26A); }
    50% { opacity: 0.5; transform: scale(0.85); box-shadow: 0 0 0 transparent; }
  }
  .ticker-track { overflow: hidden; width: 100%; }
  .ticker-inner { display: flex; gap: 0; width: max-content; animation: tickerScroll var(--ticker-duration, 30s) linear infinite; will-change: transform; }
  @keyframes tickerScroll { 0% { transform: translateX(0); } 100% { transform: translateX(-50%); } }
  .ticker-item--recent {
    border-color: var(--hf-accent, #00D26A) !important;
    background: color-mix(in srgb, var(--hf-accent) 10%, var(--hf-bg)) !important;
    box-shadow: 0 0 24px color-mix(in srgb, var(--hf-accent) 12%, transparent);
  }
  html[data-theme-mode="light"] .ticker-item--recent {
    background: color-mix(in srgb, var(--hf-accent) 12%, white) !important;
  }
  .ticker-item--recent .ticker-title {
    color: var(--hf-accent, #00D26A) !important;
    font-weight: 500 !important;
  }
  .ticker-item {
    display: flex; align-items: center; gap: 10px;
    padding: 6px 16px 6px 12px; margin: 0 8px;
    white-space: nowrap; flex-shrink: 0; font-size: 0.85rem;
    color: var(--hf-text-secondary, #ccc);
    border: 1px solid; border-radius: 100px;
    background: color-mix(in srgb, var(--hf-bg) 60%, transparent);
    backdrop-filter: blur(4px); -webkit-backdrop-filter: blur(4px);
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
  }
  .ticker-item:hover { box-shadow: 0 0 20px color-mix(in srgb, currentColor 15%, transparent); }
  html[data-theme-mode="light"] .ticker-item { background: color-mix(in srgb, var(--hf-bg-secondary) 80%, transparent); color: var(--hf-text-muted, #555); }
  .ticker-dot { width: 10px; height: 10px; border-radius: 50%; flex-shrink: 0; display: inline-block; }
  @keyframes tickerPulseRecent { 0%, 100% { transform: scale(1); opacity: 1; } 50% { transform: scale(1.3); opacity: 0.6; } }
  @keyframes tickerPulseOld { 0%, 100% { transform: scale(1); opacity: 0.6; } 50% { transform: scale(1.1); opacity: 0.3; } }
  .ticker-meta { display: inline-flex; align-items: center; gap: 8px; flex-shrink: 0; }
  .ticker-fecha { font-family: 'JetBrains Mono', monospace; font-size: 0.65rem; font-weight: 600; color: var(--hf-text-dim, #666); letter-spacing: 0.04em; text-transform: uppercase; }
  .ticker-sep { display: none; }
  .ticker-tag { font-size: 0.6rem; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; padding: 2px 8px; border-radius: 100px; border: 1px solid; }
  .ticker-title { font-weight: 400; color: var(--hf-text, #fff); max-width: 360px; overflow: hidden; text-overflow: ellipsis; font-size: 0.82rem; }
  html[data-theme-mode="light"] .ticker-title { color: var(--hf-text-secondary, #333); }
</style>

<!-- Líneas de Indagación Section -->
<section id="lineas" class="services">
  <div class="container">
    <div class="section-header">
      <span class="section-tag">{t('services.tag')}</span>
      <h2 class="section-title">{t('services.title')}</h2>
      <p class="section-subtitle">{t('services.subtitle')}</p>
    </div>
    <div class="services-chronograph">
      <!-- Background Connecting Mesh (Complex System Aristas) -->
      <div class="chrono-mesh" aria-hidden="true">
        <svg viewBox="0 0 1000 400" preserveAspectRatio="none">
          <!-- Horizontal Connections -->
          <path d="M125,125 Q250,75 375,125 T625,125 T875,125" class="mesh-line" />
          <path d="M125,325 Q250,275 375,325 T625,325 T875,325" class="mesh-line" />
          <!-- Vertical/Diagonal Cross-link -->
          <path d="M125,125 C250,125 250,325 375,325" class="mesh-line mesh-line--alt" />
          <path d="M375,125 C500,125 500,325 625,325" class="mesh-line mesh-line--alt" />
          <path d="M625,125 C750,125 750,325 875,325" class="mesh-line mesh-line--alt" />
          <path d="M875,125 C750,125 750,325 625,325" class="mesh-line mesh-line--alt" />
        </svg>
      </div>

      <div class="chronograph-grid">
        {services.map((service, index) => (
          <div class="chrono-node" style={`--chrono-color: ${(serviceColor as Record<string, string>)[service.key]}; --delay: ${index * 0.1}s`}>
            <article class="chrono-unit">
              <!-- Precision Rims (Watchmaking aesthetic) -->
              <div class="chrono-rim-outer"></div>
              <div class="chrono-rim-inner"></div>
              <div class="chrono-cat-badge" style={`--cat-color: ${service.category === 'digital' ? 'var(--hf-accent)' : service.category === 'human' ? 'var(--hf-accent-human)' : 'var(--hf-accent-analysis)'}`}>{t('services.category.' + service.category)}</div>
              <div class="chrono-content">
                <div class="chrono-icon" set:html={service.icon} />
                <h3 class="chrono-title">{t(`services.${service.key}.title`)}</h3>
              </div>

              <!-- Tooltip: Technical Spec Sheet -->
              <div class="chrono-spec">
                <div class="spec-header">
                  <span class="spec-code">HF-{service.key.substring(0,3).toUpperCase()}-{(index+1).toString().padStart(3,'0')}</span>
                  <h4>{t(`services.${service.key}.title`)}</h4>
                </div>
                <p>{t(`services.${service.key}.desc`)}</p>
                <div class="spec-footer">
                  {service.link ? (
                    <a href={service.link} class="spec-link">{t(service.linkText || 'services.explore')}</a>
                  ) : (
                    <a href="#contact" class="spec-link">{t('services.bookConsultation')}</a>
                  )}
                </div>
              </div>
            </article>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<!-- About Section -->
<section id="grupo" class="about">
  <div class="container">
    <div class="section-header">
      <span class="section-tag">{t('about.tag')}</span>
      <h2 class="section-title">{t('about.title')}</h2>
    </div>
    <p class="about-text" style="text-align:center;max-width:720px;margin:0 auto 48px;">{t('about.text1')}</p>
    <p class="about-text" style="text-align:center;max-width:720px;margin:0 auto 48px;">{t('about.text2')}</p>
    
    <!-- Member grid -->
    <div class="member-grid">
      <!-- Marcelo — perfil bajo -->
      <div class="member-card">
        <div class="member-photo"><img src="/assets/marcelo-burgos.webp" alt="Marcelo Burgos" class="member-photo-img" width="120" height="120"/></div>
        <h3 class="member-name">Marcelo Burgos</h3>
        <p class="member-role">{t('member.marcelo.role')}</p>
        <p class="member-desc">{t('member.marcelo.desc')}</p>
        <div class="member-lenses">
          <span class="m-lens m-lens--adversarial" title={locale === 'es' ? 'Adversarial · Red Team, OSINT/CTI y arquitectura ofensiva' : 'Adversarial · Red Team, OSINT/CTI & offensive architecture'}>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="12" height="12"><rect x="4" y="4" width="16" height="16" rx="2"/><circle cx="12" cy="12" r="4"/></svg>
            <span>Adversarial</span>
          </span>
          <span class="m-lens m-lens--sistemico" title={locale === 'es' ? 'Sistémico · Filosofía de la ciencia e IA y complejidad organizacional' : 'Systemic · Philosophy of science & AI and organizational complexity'}>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="12" height="12"><circle cx="12" cy="12" r="10"/><polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88"/></svg>
            <span>{locale === 'es' ? 'Sistémico' : 'Systemic'}</span>
          </span>
        </div>
      </div>
      <!-- Victor Cayupil — Partner Ágil Chile -->
      <div class="member-card">
        <div class="member-photo"><img src="/assets/victor-cayupil.webp" alt="Victor Cayupil" class="member-photo-img" width="120" height="120"/></div>
        <h3 class="member-name">{t('member.victor.name')}</h3>
        <p class="member-role">{t('member.victor.role')}</p>
        <p class="member-desc">{t('member.victor.desc')}</p>
        <div class="member-lenses">
          <span class="m-lens m-lens--gobierno" title={locale === 'es' ? 'Gobierno · NIST, ISO 27001, PCI, GDPR y marco normativo' : 'Governance · NIST, ISO 27001, PCI, GDPR & compliance'}>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="12" height="12"><circle cx="12" cy="12" r="10"/><polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88"/></svg>
            <span>{locale === 'es' ? 'Gobierno' : 'Governance'}</span>
          </span>
          <span class="m-lens m-lens--operacion" title={locale === 'es' ? 'Operación · SOC/CSIRT, IAM/PAM y Cloud/DevSecOps' : 'Operations · SOC/CSIRT, IAM/PAM & Cloud/DevSecOps'}>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="12" height="12"><rect x="4" y="4" width="16" height="16" rx="2"/><circle cx="12" cy="12" r="4"/></svg>
            <span>{locale === 'es' ? 'Operación' : 'Operations'}</span>
          </span>
        </div>
      </div>
      <!-- Cinthia González -->
      <div class="member-card">
        <div class="member-photo"><img src="/assets/cinthia-gonzalez.webp" alt="Cinthia González" class="member-photo-img" width="120" height="120"/></div>
        <h3 class="member-name">Cinthia González</h3>
        <p class="member-role">{t('member.cinthia.role')}</p>
        <p class="member-desc">{t('member.cinthia.desc')}</p>
        <div class="member-lenses">
          <span class="m-lens m-lens--human" title={t('about.panel.human')}>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="12" height="12"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/></svg>
            <span>{t('about.panel.human')}</span>
          </span>
          <span class="m-lens m-lens--social" title={t('about.panel.social')}>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="12" height="12"><circle cx="12" cy="12" r="10"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10"/></svg>
            <span>{t('about.panel.social')}</span>
          </span>
        </div>
      </div>
      <!-- Daniel Mella -->
      <div class="member-card">
        <div class="member-avatar"><svg viewBox="0 0 80 80" fill="none"><circle cx="40" cy="40" r="38" stroke="var(--hf-accent)" stroke-width="1" fill="none" opacity="0.2"/><circle cx="40" cy="30" r="12" stroke="var(--hf-accent)" stroke-width="1" fill="none" opacity="0.3"/><path d="M18 64c0-12 10-22 22-22s22 10 22 22" stroke="var(--hf-accent)" stroke-width="1" fill="none" opacity="0.3"/></svg></div>
        <div class="member-photo" style="display:none;"><img src="" alt="" class="member-photo-img"/></div>
        <h3 class="member-name">{t('member.daniel.name')}</h3>
        <p class="member-role">{t('member.daniel.role')}</p>
        <p class="member-desc">{t('member.daniel.desc')}</p>
        <div class="member-lenses">
          <span class="m-lens m-lens--social" title={t('about.panel.social')}>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="12" height="12"><circle cx="12" cy="12" r="10"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10"/></svg>
            <span>{t('about.panel.social')}</span>
          </span>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Socios Section -->
<section class="partners">
  <div class="container">
    <div class="section-header">
      <span class="section-tag">{t('partners.tag')}</span>
      <h2 class="section-title">{t('partners.title')}</h2>
      <p class="section-subtitle">{t('partners.subtitle')}</p>
    </div>
    <div class="partners-grid">
      <a href="https://agilchile.com" target="_blank" rel="noopener" class="partner-item">
        <div class="partner-icon">
          <img src="/assets/agilchile-logo.webp" alt="Ágil Chile logo" width="72" height="72" class="partner-logo-img" />
        </div>
        <h3 class="partner-name">Ágil Chile</h3>
        <p class="partner-desc">{t('partners.agilChile.desc')}</p>
      </a>
      <div class="partner-item partner-item--placeholder">
        <div class="partner-icon">
          <svg viewBox="0 0 80 80" fill="none"><circle cx="40" cy="40" r="30" stroke="var(--hf-border)" stroke-width="1" fill="none" stroke-dasharray="4 4"/><text x="40" y="46" text-anchor="middle" fill="var(--hf-text-dim)" font-size="28" font-family="Space Grotesk,sans-serif" font-weight="300">+</text></svg>
        </div>
        <h3 class="partner-name">{t('partners.upcoming.title')}</h3>
        <p class="partner-desc">{t('partners.upcoming.desc')}</p>
      </div>
    </div>
  </div>
</section>

<!-- Values Section -->
<section id="values" class="values">
  <div class="container">
    <div class="section-header">
      <span class="section-tag">{t('values.tag')}</span>
      <h2 class="section-title">{t('values.title')}</h2>
      <p class="section-subtitle">{t('values.subtitle')}</p>
    </div>
    <div class="values-grid">
      {values.map((value, index) => (
        <div class="value-card" style={`--delay: ${index * 0.15}s`}>
          <div class="value-icon" set:html={value.icon} />
          <h3 class="value-title">{t(value.titleKey)}</h3>
          <p class="value-description">{t(value.descKey)}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<!-- CTA Section -->
<section id="contact" class="cta">
  <div class="cta-bg">
    <div class="cta-glow"></div>
  </div>
  <div class="container">
    <div class="cta-content">
      <span class="section-tag">{t('cta.tag')}</span>
      <h2 class="cta-title">{t('cta.title')}</h2>
      <p class="cta-subtitle">{t('cta.subtitle')}</p>
      <div class="cta-features">
        <div class="cta-feature">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
            <polyline points="22 4 12 14.01 9 11.01"/>
          </svg>
          <span>{t('cta.feature1')}</span>
        </div>
        <div class="cta-feature">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
            <polyline points="22 4 12 14.01 9 11.01"/>
          </svg>
          <span>{t('cta.feature2')}</span>
        </div>
        <div class="cta-feature">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
            <polyline points="22 4 12 14.01 9 11.01"/>
          </svg>
          <span>{t('cta.feature3')}</span>
        </div>
      </div>
      
      <!-- Problem Form -->
      <form id="problem-form" class="problem-form"
        data-msg-required={t('form.required')}
        data-msg-email-invalid={t('form.emailInvalid')}
        data-msg-sending={t('form.sending')}
        data-msg-success={t('form.success')}
        data-msg-error={t('form.error')}
        data-msg-network={t('form.networkError')}>
        <div class="problem-form-row">
          <input type="text" id="problem-name" placeholder={t('form.name')} aria-label={t('form.name')} />
          <input type="email" id="problem-email" placeholder={t('form.email')} required aria-label={t('form.email')} autocomplete="email" />
        </div>
        <div class="problem-form-row">
          <select id="problem-area" aria-label={t('form.area')}>
            <option value="">{t('form.areaDefault')}</option>
            <option value="security">{t('form.area.security')}</option>
            <option value="architecture">{t('form.area.architecture')}</option>
            <option value="behavior">{t('form.area.behavior')}</option>
            <option value="intelligence">{t('form.area.intelligence')}</option>
            <option value="other">{t('form.area.other')}</option>
          </select>
          <select id="problem-contact" aria-label={t('form.contact')}>
            <option value="email">{t('form.contact.email')}</option>
            <option value="meeting">{t('form.contact.meeting')}</option>
            <option value="depends">{t('form.contact.depends')}</option>
          </select>
        </div>
        <div class="problem-form-row">
          <textarea id="problem-description" rows="5" placeholder={t('form.description')} required aria-label={t('form.description')}></textarea>
        </div>
        <div class="cta-actions">
          <button type="submit" class="btn btn-primary btn-large">
            <span>{t('cta.button')}</span>
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
              <polyline points="22 4 12 14.01 9 11.01"/>
            </svg>
          </button>
        </div>
        <div id="problem-form-message" class="form-message"></div>
      </form>
      
      <p class="cta-note">{t('cta.note')}</p>
    </div>
  </div>
</section>

<!-- Footer -->
<footer class="footer">
  <div class="container">
    <div class="footer-content">
      <div class="footer-brand">
        <a href={base} class="logo footer-logo" aria-label={locale === 'es' ? 'Hackfluency - Ir al inicio' : 'Hackfluency - Go to homepage'}>
          <img src={`${base}HFNeon.webp`} alt="Hackfluency logo" width="56" class="footer-logo-img" />
          <span class="logo-text"><span class="logo-hack">Hack</span><span class="logo-fluency">fluency</span></span>
        </a>
        <p class="footer-tagline">{t('footer.tagline')}</p>
      </div>
      <div class="footer-links">
        <nav class="footer-column" aria-label={t('footer.servicesTitle')}>
          <h3>{t('footer.servicesTitle')}</h3>
          <a href={locale === 'es' ? '#lineas' : '/en#lineas'}>{t('services.strategicAdvisory.title')}</a>
          <a href={locale === 'es' ? '#lineas' : '/en#lineas'}>{t('services.securityEngineering.title')}</a>
          <a href={locale === 'es' ? '#lineas' : '/en#lineas'}>{t('services.offensiveSecurity.title')}</a>
          <a href={locale === 'es' ? '#lineas' : '/en#lineas'}>{t('services.aiSystems.title')}</a>
          <a href={locale === 'es' ? '#lineas' : '/en#lineas'}>{t('services.training.title')}</a>
          <a href={locale === 'es' ? '#lineas' : '/en#lineas'}>{t('services.research.title')}</a>
        </nav>
        <nav class="footer-column" aria-label={t('footer.companyTitle')}>
          <h3>{t('footer.companyTitle')}</h3>
          <a href="#grupo">{t('footer.aboutUs')}</a>
          <a href="#values">{t('nav.approach')}</a>
          <a href="/SecurityRoadmap.HF">{t('footer.strategyRoadmap')}</a>
          <a href={locale === 'es' ? '#contact' : '/en#contact'}>{t('common.bookCall')}</a>
        </nav>
        <nav class="footer-column" aria-label={t('footer.connectTitle')}>
          <h3>{t('footer.connectTitle')}</h3>
<a href="https://www.linkedin.com/company/hackfluency" target="_blank" rel="noopener noreferrer" aria-label={locale === 'es' ? 'Sigue a Hackfluency en LinkedIn' : 'Follow Hackfluency on LinkedIn'}>LinkedIn</a>
  <a href="https://matrix.to/#/#eXcomm:matrix.org" target="_blank" rel="noopener noreferrer" aria-label={locale === 'es' ? 'Únete a nuestra comunidad de Matrix' : 'Join our Matrix community'}>Matrix</a>
        </nav>
      </div>
      </div>

      <!-- Newsletter Subscription -->
      <div class="newsletter-section">
        <h4 class="newsletter-title">{t('footer.newsletter.title')}</h4>
        <p class="newsletter-subtitle">{t('footer.newsletter.subtitle')}</p>
        <form id="subscribe-form" class="subscribe-form"
          data-msg-required={t('footer.newsletter.required')}
          data-msg-too-long={t('footer.newsletter.tooLong')}
          data-msg-invalid-chars={t('footer.newsletter.invalidChars')}
          data-msg-invalid-format={t('footer.newsletter.invalidFormat')}
          data-msg-valid={t('footer.newsletter.validEmail')}
          data-msg-subscribing={t('footer.newsletter.subscribing')}
          data-msg-success={t('footer.newsletter.success')}
          data-msg-already={t('footer.newsletter.alreadySubscribed')}
          data-msg-error={t('footer.newsletter.error')}
          data-msg-network={t('footer.newsletter.networkError')}>
          <input
            type="email"
            id="email-input"
            placeholder={t('footer.newsletter.placeholder')}
            required
            aria-label={t('footer.newsletter.ariaLabel')}
            autocomplete="email"
          />
          <button type="submit" class="subscribe-btn">{t('footer.newsletter.button')}</button>
          <p id="form-message" class="form-message"></p>
        </form>
      </div>

    <!-- Trustpilot — currently in progress -->
    <div class="trustpilot-section" style="display:none;">
      <div class="trustpilot-inner">
        <div class="trustpilot-header">
          <p class="trustpilot-label">{t('footer.trustpilot.label')}</p>
        </div>
        <div class="trustpilot-widget"
             data-locale={locale === 'es' ? 'es-ES' : 'en-US'}
             data-template-id="56278e9abfbbba0bdcd568bc"
             data-businessunit-id="68f53cf5ee195b73d33884e2"
             data-style-height="52px"
             data-style-width="100%"
             data-token="9de2933f-e5a7-4a91-936a-a96daf9fce17">
          <a href="https://www.trustpilot.com/review/hackfluency.com" target="_blank" rel="noopener">{t('footer.trustpilot.review')}</a>
        </div>
      </div>
    </div>

    <div class="footer-bottom">
      <p>{t('common.copyright')}</p>
      <a href="mailto:inquiry@hackfluency.com" class="footer-email">{t('common.email')}</a>
    </div>
  </div>
</footer>
</main>

<style>
  /* Navigation */
  .nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    padding: 20px 0;
    background: var(--hf-bg);
    border-bottom: 1px solid transparent;
    transition: all 0.3s ease;
  }

  .nav.scrolled {
    border-bottom-color: var(--hf-border);
    box-shadow: 0 4px 20px rgba(0,0,0,0.4);
  }

  .nav-container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 24px;
    display: flex;
    align-items: center;
    gap: clamp(16px, 2.5vw, 40px);
  }

  .logo {
    font-family: 'Space Grotesk', sans-serif;
    font-size: clamp(1.2rem, 2.5vw, 1.75rem);
    font-weight: 700;
    display: flex;
    align-items: center;
    gap: clamp(8px, 1.5vw, 16px);
    flex-shrink: 0;
  }

  .logo-img {
    width: clamp(36px, 4vw, 56px);
    height: auto;
    filter: none;
  }

  .logo-text {
    display: flex;
  }

  .logo-hack {
    color: var(--hf-accent);
    text-shadow: none;
    font-weight: 800;
  }

  .logo-fluency {
    color: var(--hf-text);
  }

  .nav-links {
    display: none;
    flex: 1;
    justify-content: center;
    gap: clamp(12px, 3.5vw, 40px);
  }

  @media (min-width: 768px) {
    .nav-links {
      display: flex;
    }
  }

  .nav-actions {
    display: none;
    align-items: center;
    gap: 8px;
    margin-left: auto;
    flex-shrink: 0;
  }

  @media (min-width: 768px) {
    .nav-actions {
      display: flex;
    }
  }

  .nav-links a {
    font-size: 0.9rem;
    color: var(--gray-300);
    transition: color 0.3s ease;
    padding: 12px 8px;
    min-height: 44px;
    display: flex;
    align-items: center;
  }

  .nav-links a:hover {
    color: var(--primary);
  }

  .nav-links a.nav-research {
    color: var(--primary);
    font-weight: 600;
    position: relative;
  }

  .nav-links a.nav-research::after {
    content: '';
    position: absolute;
    bottom: 6px;
    left: 8px;
    right: 8px;
    height: 1px;
    background: var(--primary);
    opacity: 0.35;
    border-radius: 1px;
  }

  .nav-links a.nav-research:hover {
    color: var(--primary);
    opacity: 0.85;
  }

  .nav-cta {
    display: none;
    padding: 12px 24px;
    background: var(--gradient-primary);
    border-radius: 8px;
    font-size: 0.875rem;
    font-weight: 700;
    color: #ffffff !important;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    min-height: 44px;
  }

  @media (min-width: 768px) {
    .nav-cta {
      display: flex;
      align-items: center;
      justify-content: center;
    }
  }

  .nav-cta:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 40px var(--primary-glow);
    color: var(--hf-text-on-accent);
  }

  .nav-btn-threat {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 10px 18px;
    background: linear-gradient(135deg, #0099FF 0%, #00B4D8 100%);
    border: none;
    border-radius: 8px;
    font-size: 0.8rem;
    font-weight: 600;
    color: var(--hf-text);
    transition: all 0.3s ease;
    min-height: 44px;
    text-decoration: none;
    box-shadow: 0 2px 12px rgba(0, 153, 255, 0.3);
  }

  .nav-btn-threat:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 24px rgba(0, 153, 255, 0.5);
    background: linear-gradient(135deg, #00B4D8 0%, #0099FF 100%);
  }

  .nav-btn-dashboard {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 12px 20px;
    background: transparent;
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 8px;
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--white);
    transition: all 0.3s ease;
    min-height: 44px;
    text-decoration: none;
  }

  .nav-btn-dashboard:hover {
    background: rgba(255, 255, 255, 0.05);
    border-color: rgba(255, 255, 255, 0.3);
    transform: translateY(-2px);
  }

  .mobile-menu-btn {
    display: flex;
    flex-direction: column;
    gap: 5px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 5px;
  }

  @media (min-width: 768px) {
    .mobile-menu-btn {
      display: none;
    }
  }

  .mobile-menu-btn span {
    width: 24px;
    height: 2px;
    background: var(--hf-text);
    transition: all 0.3s ease;
    border-radius: 2px;
  }

  /* Hamburger Button Active State */
  .mobile-menu-btn.active span:nth-child(1) {
    transform: rotate(45deg) translate(5px, 5px);
  }

  .mobile-menu-btn.active span:nth-child(2) {
    opacity: 0;
  }

  .mobile-menu-btn.active span:nth-child(3) {
    transform: rotate(-45deg) translate(5px, -5px);
  }

  .mobile-menu-overlay {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.92);
    z-index: 999;
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
  }

  .mobile-menu-overlay.active {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .mobile-menu-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 40px;
    padding: 40px;
  }

  .mobile-nav-links {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 24px;
  }

  .mobile-nav-link {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 1.5rem;
    font-weight: 600;
    color: #f0f0f0;
    text-decoration: none;
    transition: color 0.2s ease;
    padding: 12px;
    min-height: 44px;
    display: flex;
    align-items: center;
  }

  .mobile-nav-link:hover {
    color: var(--hf-accent);
  }

  .mobile-nav-link--research {
    color: var(--hf-accent);
  }

  .mobile-nav-actions {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 16px;
  }
</style>

<style is:global>
  /* Mobile menu — language switcher contrast on dark overlay */
  .mobile-menu-overlay.active ~ * .lang-switcher,
  .mobile-menu-overlay.active .lang-switcher {
    background: rgba(255, 255, 255, 0.08) !important;
    border-color: rgba(255, 255, 255, 0.2) !important;
    color: #e0e0e0 !important;
  }
  .mobile-menu-overlay.active ~ * .lang-switcher:hover,
  .mobile-menu-overlay.active .lang-switcher:hover {
    background: rgba(255, 255, 255, 0.14) !important;
    border-color: var(--hf-accent) !important;
    color: var(--hf-accent) !important;
  }
</style>

<style>
  /* Hero Section */
  .hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    position: relative;
    padding: 120px 0 80px;
    overflow: hidden;
    border-bottom: 1px solid var(--hf-border);
    background: var(--hf-bg);
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
    background-image: 
      linear-gradient(30deg, var(--hf-border) 1px, transparent 1px),
      linear-gradient(150deg, var(--hf-border) 1px, transparent 1px);
    background-size: 40px 69.28px;
    opacity: 0.1;
    pointer-events: none;
  }

  .hero-abstract {
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    opacity: 0.15;
    mix-blend-mode: screen;
  }

  @media (max-width: 767px) {
    .hero-abstract { opacity: 0.08; }
  }

  .hero-abstract-svg {
    width: 100%;
    height: 100%;
  }

  .abs-ring {
    fill: none;
    stroke-width: 0.8;
    opacity: 0.6;
  }
  .abs-ring--human { stroke: var(--hf-accent-human, #9C5E4D); }
  .abs-ring--privacy { stroke: var(--hf-accent-privacy, #1E3A5F); }
  .abs-ring--analysis { stroke: var(--hf-accent-analysis, #6C4F9E); }
  .abs-ring--philosophy { stroke: var(--hf-accent-philosophy, #7A6A4E); }

  .abs-strand {
    stroke: var(--hf-text);
    stroke-width: 0.5;
    opacity: 0.12;
    stroke-dasharray: 3 6;
  }
  .abs-strand--dim { opacity: 0.06; }

  .hero-content {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1fr;
    gap: 48px;
    align-items: center;
    width: 100%;
    padding-bottom: 80px;
  }

  @media (min-width: 1024px) {
    .hero-content {
      grid-template-columns: 1fr 1fr;
      text-align: left;
      gap: 64px;
    }
  }

  .hero-text {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
  }

  @media (max-width: 1023px) {
    .hero-text {
      align-items: center;
      text-align: center;
    }
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 16px;
    background: var(--hf-accent-dim);
    border: 1px solid var(--hf-accent-border);
    border-radius: 100px;
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--primary);
    margin-bottom: 28px;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .badge-dot {
    width: 8px;
    height: 8px;
    background: var(--primary);
    border-radius: 50%;
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  .hero-title {
    font-size: clamp(2.5rem, 8vw, 5rem);
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 24px;
    letter-spacing: -0.02em;
  }

  .hero-title-accent {
    color: var(--hf-accent);
    font-family: 'Source Serif 4', 'Georgia', serif;
    font-weight: 600;
    font-style: italic;
    display: block;
  }

  .hero-subtitle {
    font-size: clamp(1rem, 1.8vw, 1.15rem);
    color: var(--gray-400);
    max-width: 540px;
    margin: 0 0 40px;
    line-height: 1.75;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    margin-bottom: 0;
  }

  @media (max-width: 1023px) {
    .hero-actions {
      justify-content: center;
    }
  }

  .hero-proof {
    display: inline-flex;
    align-items: center;
    gap: 14px;
    margin-top: 22px;
    padding: 13px 18px;
    border: 1px solid color-mix(in srgb, var(--hf-text) 12%, transparent);
    border-radius: 12px;
    background: color-mix(in srgb, var(--hf-text) 4%, transparent);
    text-decoration: none;
    cursor: pointer;
    transition: border-color 0.25s ease, background 0.25s ease;
  }

  .hero-proof:hover {
    border-color: color-mix(in srgb, var(--hf-text) 22%, transparent);
    background: color-mix(in srgb, var(--hf-text) 7%, transparent);
  }

  @media (max-width: 1023px) {
    .hero-proof {
      width: 100%;
      max-width: 360px;
    }
  }

  .hero-proof-icon {
    width: 38px;
    height: 38px;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px solid var(--hf-accent-border);
    border-radius: 9px;
    background: var(--hf-accent-dim);
  }

  .hero-proof-icon svg {
    width: 18px;
    height: 18px;
    stroke: var(--primary);
  }

  .hero-proof-text {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .hero-proof-label {
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.09em;
    text-transform: uppercase;
    color: var(--primary);
    opacity: 0.75;
  }

  .hero-proof-link {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--white);
    transition: color 0.2s ease;
  }

  .hero-proof:hover .hero-proof-link {
    color: var(--primary);
  }

  /* Buttons */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    padding: 16px 32px;
    border-radius: 10px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    border: none;
    min-height: 48px;
    min-width: 48px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    position: relative;
    overflow: hidden;
  }

  .btn::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(255,255,255,0.12) 0%, transparent 50%);
    pointer-events: none;
    border-radius: inherit;
  }

  .btn-primary {
    background: linear-gradient(135deg, var(--hf-accent) 0%, color-mix(in srgb, var(--hf-accent) 80%, #000) 100%);
    color: #ffffff;
    box-shadow: 0 4px 20px color-mix(in srgb, var(--hf-accent) 35%, transparent);
  }

  .btn-primary span,
  .btn-primary svg {
    position: relative;
    z-index: 1;
  }

  .btn-primary span {
    color: #ffffff;
    font-weight: 700;
  }

  .btn-primary svg {
    stroke: var(--hf-bg);
  }

  .btn-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 32px color-mix(in srgb, var(--hf-accent) 45%, transparent);
    color: #ffffff;
  }

  .btn-secondary {
    background: transparent;
    color: var(--white);
    border: 1px solid color-mix(in srgb, var(--hf-text) 20%, transparent);
    backdrop-filter: blur(4px);
  }

  .btn-secondary:hover {
    background: color-mix(in srgb, var(--hf-text) 6%, transparent);
    border-color: color-mix(in srgb, var(--hf-text) 35%, transparent);
    transform: translateY(-2px);
  }

  .btn-ghost {
    background: color-mix(in srgb, var(--hf-text) 4%, transparent);
    color: var(--gray-300);
    border: 1px solid color-mix(in srgb, var(--hf-text) 10%, transparent);
    font-size: 0.95rem;
    backdrop-filter: blur(4px);
  }

  .btn-ghost:hover {
    color: var(--hf-accent);
    border-color: color-mix(in srgb, var(--hf-accent) 40%, transparent);
    background: color-mix(in srgb, var(--hf-accent) 8%, transparent);
    transform: translateY(-2px);
  }

  .btn-large {
    padding: 20px 48px;
    font-size: 1.1rem;
  }

  .btn-full {
    width: 100%;
  }

  /* Architecture diagram */
  .hero-diagram {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .arch-diagram {
    width: 100%;
    max-width: 420px;
    height: auto;
  }

  .arch-line {
    stroke: var(--hf-accent);
    stroke-width: 1.5;
    opacity: 0.35;
  }

  .arch-line--dim {
    opacity: 0.18;
    stroke-dasharray: 4 4;
  }
  .arch-line--cross {
    opacity: 0.12;
    stroke-dasharray: 2 8;
    stroke-width: 1;
  }

  .arch-node {
    fill: none;
    stroke: var(--hf-accent);
    stroke-width: 1.5;
    opacity: 0.5;
  }

  .arch-node--primary {
    opacity: 0.8;
    stroke-width: 2;
  }

  .arch-node--secondary {
    opacity: 0.6;
  }

  .arch-node--organic {
    stroke: var(--hf-accent-human);
    opacity: 0.6;
  }

  .arch-node--abstract {
    stroke: var(--hf-accent-philosophy);
    opacity: 0.5;
  }

  .arch-node--tertiary {
    opacity: 0.4;
  }

  .arch-node-inner {
    fill: var(--hf-accent);
    stroke: none;
    opacity: 0.12;
  }

  .arch-pulse {
    fill: none;
    stroke: var(--hf-accent);
    stroke-width: 1;
    opacity: 0;
    animation: archPulse 3s ease-out infinite;
  }

  .arch-pulse--delay1 {
    animation-delay: 1s;
  }

  .arch-pulse--delay2 {
    animation-delay: 2s;
  }

  .arch-pulse--organic {
    stroke: var(--hf-accent-human);
  }

  @keyframes archPulse {
    0% { opacity: 0.6; transform: scale(1); }
    100% { opacity: 0; transform: scale(1.15); }
  }

  .arch-label {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 11px;
    font-weight: 600;
    fill: var(--hf-text-muted);
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  /* Sections */
  section {
    padding: 120px 0;
  }

  .section-header {
    text-align: center;
    max-width: 700px;
    margin: 0 auto 80px;
  }

  .section-tag {
    display: inline-block;
    padding: 6px 18px;
    background: linear-gradient(135deg, var(--hf-accent-dim) 0%, color-mix(in srgb, var(--hf-accent) 5%, transparent) 100%);
    border: 1px solid color-mix(in srgb, var(--hf-accent) 20%, transparent);
    border-radius: 100px;
    font-size: 0.78rem;
    color: var(--primary);
    text-transform: uppercase;
    letter-spacing: 0.12em;
    margin-bottom: 20px;
    font-weight: 600;
  }

  .section-title {
    font-size: clamp(2rem, 5vw, 3rem);
    margin-bottom: 20px;
  }

  .section-subtitle {
    font-size: 1.05rem;
    color: var(--gray-400);
    line-height: 1.7;
    max-width: 600px;
    margin: 0 auto;
  }



  /* Services */
  .services {
    background: var(--dark);
  }

  /* ── Services Chronograph (High-End Precision) ── */
  .services-chronograph {
    max-width: 1100px;
    margin: 20px auto 40px; /* Reduced vertical margins */
    padding: 0 24px;
    position: relative;
  }

  /* Connecting Aristas Style */
  .chrono-mesh {
    position: absolute;
    inset: 0;
    z-index: 0;
    opacity: 0.15;
    pointer-events: none;
  }

  html[data-theme-mode="light"] .chrono-mesh {
    opacity: 0.5; /* Increased container visibility for light themes */
  }
  
  .chrono-mesh svg { width: 100%; height: 100%; }
  
  .mesh-line {
    stroke: var(--hf-accent);
    stroke-width: 1;
    fill: none;
    stroke-dasharray: 4 4;
    animation: meshFlow 20s linear infinite;
  }

  html[data-theme-mode="light"] .mesh-line {
    stroke: var(--hf-text-muted); /* High contrast gray for white backgrounds */
    stroke-width: 1.2;
  }

  .mesh-line--alt {
    stroke: var(--hf-accent-human, #9C5E4D);
    opacity: 0.5;
    animation-duration: 30s;
    animation-direction: reverse;
  }

  html[data-theme-mode="light"] .mesh-line--alt {
    stroke: var(--hf-accent-human);
    stroke-width: 1;
  }

  @keyframes meshFlow {
    from { stroke-dashoffset: 200; }
    to { stroke-dashoffset: 0; }
  }

  .chronograph-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 30px;
    position: relative;
    z-index: 1;
  }

  @media (min-width: 768px) {
    .chronograph-grid { grid-template-columns: repeat(4, 1fr); gap: 40px 20px; }
  }

  /* Section Padding Reductions */
  section {
    padding: 80px 0; /* Reduced from 120px */
  }

  .section-header {
    text-align: center;
    max-width: 700px;
    margin: 0 auto 50px; /* Reduced from 80px */
  }

  .services {
    background: var(--dark);
    padding-bottom: 40px; /* Tighten bottom */
  }

  .about {
    position: relative;
    background: var(--hf-bg-secondary);
    border-bottom: 1px solid var(--hf-border);
    padding-top: 60px; /* Reduced to bring closer to services */
  }

  .chronograph-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 30px;
  }

  @media (min-width: 768px) {
    .chronograph-grid { grid-template-columns: repeat(4, 1fr); gap: 40px; }
  }

  .chrono-node {
    position: relative;
    aspect-ratio: 1;
    animation: chronoFadeIn 1s cubic-bezier(0.16, 1, 0.3, 1) both;
    animation-delay: var(--delay);
  }

  @keyframes chronoFadeIn {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .chrono-unit {
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    cursor: crosshair;
    position: relative;
  }

  .chrono-cat-badge {
    position: absolute;
    top: 8px;
    left: 50%;
    transform: translateX(-50%);
    font-size: 0.55rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--cat-color);
    opacity: 0.5;
    white-space: nowrap;
    pointer-events: none;
  }

  /* Precision Rims */
  .chrono-rim-outer,
  .chrono-rim-inner {
    position: absolute;
    inset: 0;
    border-radius: 50%;
    border: 0.5px solid color-mix(in srgb, var(--chrono-color) 20%, var(--hf-border));
    transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .chrono-rim-outer {
    border-style: dashed;
    border-width: 1px;
    animation: chronoRotate 60s linear infinite;
  }

  .chrono-rim-inner {
    inset: 15%;
    border-style: solid;
    opacity: 0.3;
    animation: chronoRotate 40s linear infinite reverse;
  }

  .chrono-unit:hover .chrono-rim-outer {
    inset: -10px;
    border-color: var(--chrono-color);
    border-style: solid;
    opacity: 0.6;
    animation-duration: 10s;
  }

  .chrono-unit:hover .chrono-rim-inner {
    inset: 10%;
    opacity: 0.8;
    border-color: var(--chrono-color);
    animation-duration: 5s;
  }

  @keyframes chronoRotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  .chrono-content {
    position: relative;
    z-index: 5;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
  }

  .chrono-icon {
    width: 32px;
    height: 32px;
    color: var(--chrono-color);
    transition: all 0.4s ease;
    filter: drop-shadow(0 0 8px color-mix(in srgb, var(--chrono-color) 30%, transparent));
  }

  .chrono-title {
    font-size: 0.65rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--hf-text-secondary);
    max-width: 125px;
    line-height: 1.3;
    transition: color 0.3s ease;
  }

  .chrono-unit:hover .chrono-title {
    color: var(--hf-text);
  }

  /* ── Technical Spec Sheet (Tooltip) ── */
  .chrono-spec {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 300px;
    padding: 24px;
    background: color-mix(in srgb, var(--hf-bg-elevated) 98%, #fff);
    backdrop-filter: blur(20px);
    border: 1px solid var(--hf-border);
    box-shadow: 0 30px 60px rgba(0,0,0,0.6);
    opacity: 0;
    visibility: hidden;
    transform: translate(-50%, -40%) scale(0.95);
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    z-index: 100;
    pointer-events: none;
  }

  @media (hover: hover) {
    .chrono-unit:hover .chrono-spec {
      opacity: 1;
      visibility: visible;
      transform: translate(-50%, -50%) scale(1);
      pointer-events: auto;
    }
  }
  .chrono-unit.active .chrono-spec {
    opacity: 1;
    visibility: visible;
    transform: translate(-50%, -50%) scale(1);
    pointer-events: auto;
  }

  .spec-header {
    margin-bottom: 16px;
    border-bottom: 1px solid var(--hf-border);
    padding-bottom: 12px;
  }

  .spec-code {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.6rem;
    color: var(--chrono-color);
    display: block;
    margin-bottom: 4px;
    letter-spacing: 0.1em;
  }

  .spec-header h4 {
    font-size: 0.9rem;
    font-weight: 700;
    text-transform: uppercase;
    color: var(--hf-text);
  }

  .chrono-spec p {
    font-size: 0.85rem;
    color: var(--gray-400);
    line-height: 1.6;
    margin-bottom: 20px;
  }

  .spec-link {
    font-size: 0.75rem;
    font-weight: 700;
    color: var(--hf-accent);
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    display: inline-block;
    border-bottom: 1px solid transparent;
    transition: all 0.3s;
  }

  .spec-link:hover {
    color: var(--hf-accent-light);
    border-bottom-color: var(--hf-accent);
  }

  @media (max-width: 767px) {
    .chrono-node {
      animation: none;
    }
    .chrono-spec {
      position: fixed;
      left: 16px;
      right: 16px;
      bottom: 16px;
      top: auto;
      width: auto;
      padding: 16px;
      border-radius: 12px;
      max-height: 40vh;
      overflow-y: auto;
      transform: translateY(20px);
    }
    .chrono-spec p { font-size: 0.8rem; margin-bottom: 12px; }
    .spec-header { margin-bottom: 10px; padding-bottom: 8px; }
    .spec-header h4 { font-size: 0.8rem; }
    .spec-code { font-size: 0.55rem; }
    @media (hover: hover) {
      .chrono-unit:hover .chrono-spec { transform: translateY(0); }
    }
    .chrono-unit.active .chrono-spec { transform: translateY(0); }
  }

  @media (min-width: 640px) {
    .services-grid {
      grid-template-columns: repeat(4, 1fr);
      gap: 24px;
    }
  }

  /* ── Gear Card ── */
  .gear-card {
    position: relative;
    background: var(--hf-bg-card);
    border: 1px solid color-mix(in srgb, var(--gear-color, var(--hf-accent)) 20%, var(--hf-border));
    border-radius: 14px;
    padding: 28px 16px 24px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 10px;
    cursor: default;
    overflow: visible;
    animation: gearFadeIn 0.6s ease-out both;
    animation-delay: var(--delay, 0s);
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
  }

  @keyframes gearFadeIn {
    from { opacity: 0; transform: translateY(20px) scale(0.95); }
    to { opacity: 1; transform: translateY(0) scale(1); }
  }

  /* ── Gear teeth top & bottom ── */
  .gear-teeth-top,
  .gear-teeth-bottom {
    position: absolute;
    left: -1px;
    right: -1px;
    height: 8px;
    background: transparent;
    overflow: hidden;
    pointer-events: none;
    z-index: 2;
  }
  .gear-teeth-top {
    top: -5px;
  }
  .gear-teeth-bottom {
    bottom: -5px;
  }

  .gear-teeth-top::before,
  .gear-teeth-bottom::before {
    content: '';
    position: absolute;
    left: 0;
    right: 0;
    height: 100%;
    background: repeating-radial-gradient(
      ellipse 6px 6px at 12px center,
      color-mix(in srgb, var(--gear-color, var(--hf-accent)) 40%, var(--hf-border)) 0px,
      color-mix(in srgb, var(--gear-color, var(--hf-accent)) 40%, var(--hf-border)) 5px,
      transparent 5px,
      transparent 10px
    );
    background-size: 24px 100%;
  }

  .gear-card:hover .gear-teeth-top::before,
  .gear-card:hover .gear-teeth-bottom::before {
    background: repeating-radial-gradient(
      ellipse 6px 6px at 12px center,
      var(--gear-color, var(--hf-accent)) 0px,
      var(--gear-color, var(--hf-accent)) 5px,
      transparent 5px,
      transparent 10px
    );
  }

  /* ── Rotor (gear SVG) ── */
  .gear-rotor {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    pointer-events: none;
    z-index: 0;
  }

  .gear-rotor svg {
    width: 85%;
    height: 85%;
    stroke: color-mix(in srgb, var(--gear-color, var(--hf-accent)) 8%, transparent);
    fill: none;
    animation: gearSpin var(--rot-speed, 6s) linear infinite;
  }

  .gear-card:hover .gear-rotor svg {
    stroke: color-mix(in srgb, var(--gear-color, var(--hf-accent)) 20%, transparent);
    animation-duration: calc(var(--rot-speed, 6s) * 0.3);
  }

  /* Each adjacent card rotates in opposite direction */
  .gear-card:nth-child(even) .gear-rotor svg {
    animation-direction: reverse;
  }

  @keyframes gearSpin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  /* ── Icon ── */
  .gear-card-icon {
    position: relative;
    z-index: 1;
    width: 42px;
    height: 42px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--gear-color, var(--hf-accent));
    transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .gear-card-icon svg {
    width: 100%;
    height: 100%;
  }

  .gear-card:hover .gear-card-icon {
    transform: scale(1.15);
  }

  /* ── Title ── */
  .gear-card-title {
    position: relative;
    z-index: 1;
    font-size: 0.8rem;
    font-weight: 700;
    text-align: center;
    color: var(--hf-text);
    line-height: 1.25;
    letter-spacing: 0.02em;
  }

  /* ── Tooltip ── */
  .gear-card-tooltip {
    position: absolute;
    z-index: 50;
    bottom: calc(100% + 14px);
    left: 50%;
    transform: translateX(-50%) translateY(6px);
    width: 240px;
    padding: 14px 18px;
    background: var(--hf-bg-elevated);
    border: 1px solid color-mix(in srgb, var(--gear-color, var(--hf-accent)) 30%, var(--hf-border));
    border-radius: 10px;
    box-shadow: 0 8px 32px rgba(0,0,0,0.35);
    opacity: 0;
    pointer-events: none;
    transition: all 0.25s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .gear-card-tooltip::after {
    content: '';
    position: absolute;
    top: 100%;
    left: 50%;
    transform: translateX(-50%);
    border: 6px solid transparent;
    border-top-color: color-mix(in srgb, var(--gear-color, var(--hf-accent)) 30%, var(--hf-border));
  }

  .gear-card:hover .gear-card-tooltip {
    opacity: 1;
    pointer-events: auto;
    transform: translateX(-50%) translateY(0);
  }

  .gear-card-desc {
    font-size: 0.8rem;
    color: var(--gray-300);
    line-height: 1.55;
    margin-bottom: 8px;
  }

  .gear-card-link {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    font-size: 0.72rem;
    font-weight: 600;
    color: var(--gear-color, var(--hf-accent));
    text-decoration: none;
    transition: gap 0.2s ease;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .gear-card-link:hover {
    gap: 9px;
  }

  @media (max-width: 639px) {
    .services-grid { gap: 14px; }
    .gear-card { padding: 20px 12px 18px; }
    .gear-card-title { font-size: 0.72rem; }
    .gear-card-icon { width: 34px; height: 34px; }
    .gear-teeth-top, .gear-teeth-bottom { height: 6px; }
    .gear-teeth-top { top: -4px; }
    .gear-teeth-bottom { bottom: -4px; }
    .gear-card-tooltip { width: 200px; bottom: calc(100% + 10px); }
  }

  /* Member Grid */
  .member-grid {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 20px;
    max-width: 1180px;
    margin: 0 auto;
  }

  .member-card {
    flex: 0 1 268px;
    max-width: 290px;
    min-width: 252px;
    background: var(--hf-bg);
    border: 1px solid var(--hf-border);
    border-radius: 12px;
    padding: 28px 20px;
    text-align: center;
    transition: all 0.3s ease;
  }

  .member-card:hover {
    border-color: color-mix(in srgb, var(--hf-accent) 40%, var(--hf-border));
    transform: translateY(-3px);
    box-shadow: 0 8px 32px color-mix(in srgb, var(--hf-accent) 8%, transparent);
  }

  .member-avatar {
    width: 120px;
    height: 120px;
    margin: 0 auto 16px;
  }
  .member-photo {
    width: 120px;
    height: 120px;
    margin: 0 auto 16px;
    position: relative;
  }
  .member-photo-img {
    width: 120px;
    height: 120px;
    border-radius: 50%;
    object-fit: cover;
    display: block;
    border: 3px solid var(--hf-border);
    box-shadow: 0 4px 20px color-mix(in srgb, var(--hf-accent) 15%, transparent);
    transition: all 0.3s ease;
    background: var(--hf-bg);
  }
  .member-card:hover .member-photo-img {
    border-color: color-mix(in srgb, var(--hf-accent) 45%, var(--hf-border));
    transform: scale(1.04);
    box-shadow: 0 8px 32px color-mix(in srgb, var(--hf-accent) 25%, transparent);
  }
  .member-lenses {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 6px;
    margin-top: 12px;
  }
  .m-lens {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    font-size: 0.55rem;
    font-weight: 700;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    padding: 3px 8px 3px 6px;
    border-radius: 100px;
    border: 1px solid;
    background: color-mix(in srgb, var(--hf-bg) 50%, transparent);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    transition: all 0.25s ease;
    position: relative;
  }
  .m-lens svg {
    width: 10px;
    height: 10px;
    flex-shrink: 0;
    opacity: 0.9;
  }
  .m-lens span {
    line-height: 1;
  }
  .member-card:hover .m-lens {
    transform: translateY(-1px);
    box-shadow: 0 0 8px color-mix(in srgb, currentColor 8%, transparent);
  }
  .m-lens--digital { color: var(--hf-accent); border-color: color-mix(in srgb, var(--hf-accent) 35%, transparent); background: linear-gradient(135deg, color-mix(in srgb, var(--hf-accent) 10%, var(--hf-bg)) 0%, color-mix(in srgb, var(--hf-accent) 3%, var(--hf-bg)) 100%); }
  .m-lens--human { color: var(--hf-accent-human); border-color: color-mix(in srgb, var(--hf-accent-human) 35%, transparent); background: linear-gradient(135deg, color-mix(in srgb, var(--hf-accent-human) 10%, var(--hf-bg)) 0%, color-mix(in srgb, var(--hf-accent-human) 3%, var(--hf-bg)) 100%); }
  .m-lens--social { color: var(--hf-accent-privacy); border-color: color-mix(in srgb, var(--hf-accent-privacy) 35%, transparent); background: linear-gradient(135deg, color-mix(in srgb, var(--hf-accent-privacy) 10%, var(--hf-bg)) 0%, color-mix(in srgb, var(--hf-accent-privacy) 3%, var(--hf-bg)) 100%); }
  .m-lens--philosophy { color: var(--hf-accent-philosophy); border-color: color-mix(in srgb, var(--hf-accent-philosophy) 35%, transparent); background: linear-gradient(135deg, color-mix(in srgb, var(--hf-accent-philosophy) 10%, var(--hf-bg)) 0%, color-mix(in srgb, var(--hf-accent-philosophy) 3%, var(--hf-bg)) 100%); }
  .m-lens--adversarial { color: var(--hf-accent-secondary); border-color: color-mix(in srgb, var(--hf-accent-secondary) 35%, transparent); background: linear-gradient(135deg, color-mix(in srgb, var(--hf-accent-secondary) 12%, var(--hf-bg)) 0%, color-mix(in srgb, var(--hf-accent-secondary) 4%, var(--hf-bg)) 100%); }
  .m-lens--sistemico { color: var(--hf-accent-analysis); border-color: color-mix(in srgb, var(--hf-accent-analysis) 35%, transparent); background: linear-gradient(135deg, color-mix(in srgb, var(--hf-accent-analysis) 12%, var(--hf-bg)) 0%, color-mix(in srgb, var(--hf-accent-analysis) 4%, var(--hf-bg)) 100%); }
  .m-lens--gobierno { color: #0E7490; border-color: color-mix(in srgb, #0E7490 35%, transparent); background: linear-gradient(135deg, color-mix(in srgb, #0E7490 12%, var(--hf-bg)) 0%, color-mix(in srgb, #0E7490 4%, var(--hf-bg)) 100%); }
  .m-lens--operacion { color: #B45309; border-color: color-mix(in srgb, #B45309 35%, transparent); background: linear-gradient(135deg, color-mix(in srgb, #B45309 12%, var(--hf-bg)) 0%, color-mix(in srgb, #B45309 4%, var(--hf-bg)) 100%); }

  .member-name {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 1rem;
    font-weight: 700;
    color: var(--hf-text);
    margin-bottom: 4px;
  }

  .member-role {
    font-size: 0.72rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--hf-accent);
    margin-bottom: 10px;
  }

  .member-desc {
    font-size: 0.82rem;
    color: var(--gray-400);
    line-height: 1.5;
  }

  /* About */
  .about {
    position: relative;
    background: var(--hf-bg-secondary);
    border-bottom: 1px solid var(--hf-border);
  }

  .about::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: 
      linear-gradient(var(--hf-border) 1px, transparent 1px),
      linear-gradient(90deg, var(--hf-border) 1px, transparent 1px);
    background-size: 60px 60px;
    opacity: 0.2;
    pointer-events: none;
  }

  .about-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 60px;
    align-items: center;
    position: relative;
  }

  @media (min-width: 1024px) {
    .about-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  .about-text {
    color: var(--gray-400);
    font-size: 1.05rem;
    line-height: 1.8;
    margin-bottom: 20px;
  }

  .about-content .section-title {
    font-family: 'Source Serif 4', 'Georgia', serif;
    font-weight: 700;
    letter-spacing: -0.02em;
  }

  .about-features {
    display: flex;
    flex-direction: column;
    gap: 16px;
    margin-top: 32px;
  }

  .about-feature {
    display: flex;
    align-items: center;
    gap: 12px;
    color: var(--gray-300);
  }

  .about-feature svg {
    color: var(--hf-accent);
    flex-shrink: 0;
  }



  /* Partners Grid */
  .partners {
    background: var(--hf-bg);
    border-top: 1px solid var(--hf-border);
    border-bottom: 1px solid var(--hf-border);
    padding: 80px 0;
  }

  .partners-grid {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 40px 60px;
    max-width: 900px;
    margin: 0 auto;
  }

  .partner-item {
    flex: 0 0 200px;
    text-align: center;
  }

  .partner-item .partner-icon {
    transition: opacity 0.3s ease;
  }

  .partner-item:hover .partner-icon {
    opacity: 1;
  }

  .partner-item--placeholder {
    cursor: default;
    pointer-events: none;
  }

  .partner-icon {
    opacity: 0.8;
  }

  .partner-item--placeholder .partner-icon {
    opacity: 0.5;
  }

  .partner-logo-img {
    width: 72px;
    height: 72px;
    object-fit: contain;
    border-radius: 12px;
  }

  .partner-item a {
    text-decoration: none;
  }

  .partner-icon {
    width: 72px;
    height: 72px;
    margin: 0 auto 12px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .partner-name {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--hf-text);
    margin-bottom: 6px;
  }

  .partner-desc {
    font-size: 0.78rem;
    color: var(--hf-text-muted);
    line-height: 1.5;
  }

  /* Values */
  .values {
    background: var(--dark);
  }

  .values-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 24px;
  }

  @media (min-width: 640px) {
    .values-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .values-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (min-width: 1280px) {
    .values-grid {
      grid-template-columns: repeat(3, 1fr);
      max-width: 900px;
      margin: 0 auto;
    }
  }

  .value-card {
    background: var(--hf-bg);
    border: 1px solid var(--hf-border);
    border-radius: 12px;
    padding: 48px 32px;
    text-align: center;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    position: relative;
    overflow: hidden;
  }

  .value-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, color-mix(in srgb, var(--hf-accent) 5%, transparent) 0%, transparent 100%);
    opacity: 0;
    transition: opacity 0.3s ease;
    border-radius: inherit;
  }

  .value-card:hover::before {
    opacity: 1;
  }

  .value-card:hover {
    background: var(--hf-bg-secondary);
    border-color: color-mix(in srgb, var(--hf-accent) 40%, var(--hf-border));
    transform: translateY(-4px);
    box-shadow: 0 12px 32px color-mix(in srgb, var(--hf-accent) 8%, transparent);
  }

  .value-card:hover .value-icon {
    background: var(--hf-accent);
    border-color: var(--hf-accent);
    color: var(--hf-bg);
    box-shadow: 0 4px 16px color-mix(in srgb, var(--hf-accent) 25%, transparent);
  }

  .value-icon {
    width: 64px;
    height: 64px;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px solid var(--hf-border);
    margin: 0 auto 32px;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    color: var(--hf-accent);
    border-radius: 16px;
  }

  .value-icon svg {
    width: 100%;
    height: 100%;
  }

  .value-title {
    font-size: 1.25rem;
    margin-bottom: 12px;
    color: var(--white);
  }

  .value-description {
    color: var(--gray-400);
    font-size: 0.9rem;
    line-height: 1.7;
  }

  /* CTA */
  .cta {
    border-top: 1px solid var(--hf-border);
    border-bottom: 1px solid var(--hf-border);
    background: linear-gradient(135deg, var(--hf-bg-secondary) 0%, var(--hf-bg) 50%, var(--hf-bg-secondary) 100%);
    position: relative;
    padding: 100px 0;
  }

  .cta-glow {
    display: none;
  }

  .cta-content {
    position: relative;
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
  }

  .cta-title {
    font-size: clamp(2rem, 5vw, 3.5rem);
    margin-bottom: 20px;
  }

  .cta-subtitle {
    color: var(--gray-300);
    font-size: 1.1rem;
    line-height: 1.7;
    margin-bottom: 32px;
  }

  .cta-features {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 24px;
    margin-bottom: 40px;
  }

  .cta-feature {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 0.9rem;
    color: var(--gray-400);
  }

  .cta-feature svg {
    color: var(--hf-accent);
    flex-shrink: 0;
  }

  .cta-actions {
    display: flex;
    justify-content: center;
    margin-bottom: 16px;
  }

  .cta-note {
    font-size: 0.8rem;
    color: var(--gray-600);
    font-style: italic;
  }

  /* Problem Form */
  .problem-form {
    max-width: 600px;
    margin: 0 auto 20px;
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .problem-form-row {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
  }

  .problem-form-row input,
  .problem-form-row select,
  .problem-form-row textarea {
    flex: 1;
    min-width: 200px;
    padding: 14px 16px;
    background: var(--hf-bg);
    border: 1px solid var(--hf-border);
    border-radius: 10px;
    color: var(--hf-text);
    font-size: 0.9rem;
    font-family: inherit;
    outline: none;
    transition: border-color 0.2s ease;
    min-height: 48px;
  }

  .problem-form-row textarea {
    width: 100%;
    min-width: 100%;
    resize: vertical;
    min-height: 120px;
  }

  .problem-form-row input:focus,
  .problem-form-row select:focus,
  .problem-form-row textarea:focus {
    border-color: var(--hf-accent);
  }

  .problem-form-row input::placeholder,
  .problem-form-row textarea::placeholder {
    color: var(--gray-600);
  }

  .problem-form-row select {
    cursor: pointer;
    appearance: auto;
  }

  .problem-form-row select option {
    background: var(--hf-bg-secondary);
    color: var(--hf-text);
  }

  /* Footer */
  .footer {
    background: var(--hf-bg-secondary);
    border-top: 1px solid var(--hf-border);
    padding: 80px 0 40px;
  }

  .footer-content {
    display: grid;
    grid-template-columns: 1fr;
    gap: 48px;
    margin-bottom: 48px;
  }

  @media (min-width: 768px) {
    .footer-content {
      grid-template-columns: 1fr 2fr;
    }
  }

  .footer-brand {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .footer-logo-img {
    width: 48px;
    height: auto;
  }

  .footer-tagline {
    font-size: 0.85rem;
    color: var(--hf-text-muted);
    line-height: 1.6;
    max-width: 300px;
  }

  .footer-links {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
    gap: 32px;
  }

  .footer-column {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .footer-column h3 {
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--hf-text-secondary);
    margin-bottom: 8px;
  }

  .footer-column a {
    font-size: 0.85rem;
    color: var(--gray-400);
    transition: color 0.2s ease;
    padding: 4px 0;
  }

  .footer-column a:hover {
    color: var(--primary);
  }

  /* Newsletter */
  .newsletter-section {
    padding: 32px 0;
    border-top: 1px solid var(--hf-border);
    border-bottom: 1px solid var(--hf-border);
    margin-bottom: 32px;
    text-align: center;
  }

  .newsletter-title {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 1.2rem;
    margin-bottom: 8px;
  }

  .newsletter-subtitle {
    font-size: 0.85rem;
    color: var(--hf-text-muted);
    margin-bottom: 20px;
  }

  .subscribe-form {
    display: flex;
    gap: 12px;
    max-width: 420px;
    margin: 0 auto;
    flex-wrap: wrap;
    justify-content: center;
  }

  .subscribe-form input {
    flex: 1;
    min-width: 200px;
    padding: 12px 16px;
    background: var(--hf-bg);
    border: 1px solid var(--hf-border);
    border-radius: 8px;
    color: var(--hf-text);
    font-size: 0.9rem;
    min-height: 44px;
    outline: none;
    transition: border-color 0.2s ease;
  }

  .subscribe-form input:focus {
    border-color: var(--hf-accent);
  }

  .subscribe-form input::placeholder {
    color: var(--gray-600);
  }

  .subscribe-btn {
    padding: 12px 24px;
    background: var(--hf-accent);
    color: var(--hf-text-on-accent);
    border: none;
    border-radius: 8px;
    font-size: 0.9rem;
    font-weight: 600;
    cursor: pointer;
    min-height: 44px;
    transition: background 0.2s ease;
    white-space: nowrap;
  }

  .subscribe-btn:hover {
    background: var(--hf-accent-light);
  }

  .form-message {
    width: 100%;
    font-size: 0.8rem;
    margin-top: 8px;
  }

  .form-message.error { color: var(--accent); }
  .form-message.success { color: var(--primary); }
  .form-message.warning { color: #f59e0b; }
  .form-message.info { color: var(--gray-400); }

  /* Trustpilot */
  .trustpilot-section {
    padding: 32px 0;
    text-align: center;
  }

  .trustpilot-inner {
    max-width: 500px;
    margin: 0 auto;
  }

  .trustpilot-header {
    margin-bottom: 20px;
  }

  .trustpilot-stars {
    display: flex;
    gap: 4px;
    justify-content: center;
    margin-bottom: 12px;
  }

  .trustpilot-stars svg {
    width: 28px;
    height: 28px;
    fill: #00b67a;
  }

  .trustpilot-label {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 1rem;
    font-weight: 600;
    margin-bottom: 4px;
  }

  .trustpilot-sub {
    font-size: 0.8rem;
    color: var(--gray-500);
  }

  .trustpilot-widget {
    margin-top: 8px;
  }

  /* Footer bottom */
  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 16px;
    padding-top: 24px;
    font-size: 0.8rem;
    color: var(--hf-text-muted);
  }

  .footer-email {
    color: var(--gray-400);
    transition: color 0.2s ease;
  }

  .footer-email:hover {
    color: var(--primary);
  }

  /* ── Paper Hero (Journal-style) ── */
  .paper-hero {
    position: relative;
    z-index: 1;
    padding: 40px 24px 20px;
  }
  .paper-head {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    align-items: center;
    gap: 12px;
    margin-bottom: 32px;
    padding-bottom: 16px;
    border-bottom: 2px solid var(--hf-border, #222);
  }
  html[data-theme-mode="light"] .paper-head {
    border-bottom-color: #d0d8e0;
  }
  .paper-meta {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  .paper-ref { color: var(--hf-accent, #00D26A); }
  .paper-sep { color: var(--hf-text-dim, #444); opacity: 0.4; }
  .paper-date { color: var(--hf-text-muted, #888); }
  .paper-tag {
    color: var(--hf-accent, #00D26A);
    border: 1px solid var(--hf-accent-border, rgba(0,210,106,0.3));
    padding: 2px 10px;
    border-radius: 100px;
    font-size: 0.6rem;
  }
  .paper-authors {
    font-size: 0.8rem;
    color: var(--hf-text-muted, #888);
  }
  .paper-author { font-weight: 600; color: var(--hf-text, #fff); }
  html[data-theme-mode="light"] .paper-author { color: var(--hf-text, #1A1A18); }
  .paper-author-div { opacity: 0.3; margin: 0 4px; }
  .paper-org { color: var(--hf-accent); text-decoration: none; }
  .paper-title-area { margin-bottom: 32px; }
  .paper-title {
    font-size: clamp(2rem, 5vw, 3.2rem);
    font-weight: 800;
    letter-spacing: -1.5px;
    line-height: 1.08;
    margin-bottom: 8px;
  }
  .paper-chart-lead {
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--hf-accent, #00D26A);
    margin-bottom: 4px;
  }
  .paper-desc {
    font-size: 0.9rem;
    color: var(--hf-text-dim, #666);
    font-weight: 400;
  }
  .paper-grid { display: flex; flex-direction: column; gap: 14px; }
  .paper-sankey {
    border-radius: 14px;
    overflow: hidden;
    border: 1px solid var(--hf-border, #222);
    background: white;
  }
  html[data-theme-mode="light"] .paper-sankey {
    background: white;
    border-color: #e0e4ea;
  }

  .hf-sankey-iframe { width: 100%; aspect-ratio: 5 / 3; border: none; display: block; }

  /* Mobile: preview image + CTA instead of iframe */
  .sankey-mobile-preview { display: none; }
  @media (max-width: 767px) {
    .hf-sankey-iframe { display: none; }
    .paper-sankey { min-height: auto; background: transparent; border: none; }
    .sankey-mobile-preview { display: flex; flex-direction: column; gap: 20px; }
    .sankey-mobile-preview img { width: 100%; height: auto; border-radius: 12px; border: 1px solid var(--hf-border, #222); }
    .sankey-mobile-cta { display: block; text-align: center; padding: 16px 20px; background: var(--hf-accent, #00D26A); color: var(--hf-text-on-accent, #000); border-radius: 12px; font-weight: 700; font-size: 1rem; text-decoration: none; transition: all 0.15s; box-shadow: 0 2px 8px rgba(0,210,106,0.25); }
    .sankey-mobile-cta:active { transform: scale(0.97); box-shadow: 0 1px 4px rgba(0,210,106,0.15); opacity: 0.85; }
  }

  .paper-findings { display: flex; flex-direction: column; gap: 16px; margin-top: 8px; }
  .findings-header {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--hf-text-muted, #888);
  }
  .findings-icon { font-size: 1rem; }
  /* Visual scorecard CTA to self-assessment */
  .diag-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; }
  @media (max-width: 600px) { .diag-grid { grid-template-columns: 1fr; } }
  .diag-chip {
    display: flex; flex-direction: column; align-items: center; gap: 6px;
    padding: 18px 12px;
    background: var(--hf-bg-card, #111);
    border: 1px solid var(--hf-border, #222);
    border-radius: 12px;
    text-align: center;
    transition: box-shadow 0.2s;
  }
  html[data-theme-mode="light"] .diag-chip { background: white; border-color: #e0e4ea; }
  .diag-chip:hover { box-shadow: 0 2px 12px rgba(0,0,0,0.06); }
  .diag-num {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 1.9rem; font-weight: 800; line-height: 1;
    color: var(--hf-accent);
  }
  .diag-unit { font-size: 0.9rem; font-weight: 700; margin-left: 2px; color: var(--hf-text-muted, #888); }
  .diag-caption { font-size: 0.75rem; color: var(--hf-text-muted, #888); line-height: 1.4; }
  .diag-actions { display: flex; flex-direction: column; gap: 10px; align-items: stretch; }
  .paper-cta {
    display: inline-block;
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--hf-accent);
    text-decoration: none;
    padding: 11px 20px;
    border: 1px solid var(--hf-accent-border, rgba(0,210,106,0.3));
    border-radius: 8px;
    text-align: center;
    background: color-mix(in srgb, var(--hf-text) 7%, transparent);
    transition: all 0.2s;
  }
  .paper-cta:hover {
    background: color-mix(in srgb, var(--hf-text) 12%, transparent);
    border-color: var(--hf-accent);
  }
  .paper-cta--primary {
    background: var(--hf-accent);
    border-color: var(--hf-accent);
    color: var(--hf-text-on-accent, #000);
    padding: 14px 26px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 2px;
    animation:
      hfCtaNudge 3.2s ease-in-out infinite,
      hfCtaGlowPrim 2.6s ease-in-out infinite;
  }
  .cta-line--main { font-size: 1.05rem; line-height: 1.2; }
  .cta-line--sub {
    font-size: 0.85rem;
    font-weight: 600;
    opacity: 0.9;
    letter-spacing: 0.02em;
  }
  .paper-cta--primary:hover {
    background: var(--hf-accent-light, var(--hf-accent));
    border-color: var(--hf-accent-light, var(--hf-accent));
    color: var(--hf-text-on-accent, #000);
    animation-play-state: paused;
  }
  /* Diferenciado: ver informe completo -> respiracion suave + glow tenue (sin salto) */
  .paper-cta:not(.paper-cta--primary) {
    animation: hfCtaBreathe 4s ease-in-out infinite;
  }
  @keyframes hfCtaNudge {
    0%, 22%, 100% { transform: translateY(0) scale(1); }
    8%   { transform: translateY(-3px) scale(1.02); }
    16%  { transform: translateY(0) scale(0.99); }
  }
  @keyframes hfCtaGlowPrim {
    0%, 100% { box-shadow: 0 0 0 0 rgba(0,210,106,0.55); }
    50%      { box-shadow: 0 0 14px 6px rgba(0,210,106,0.35); }
  }
  @keyframes hfCtaBreathe {
    0%, 100% { transform: scale(1); box-shadow: 0 0 0 0 rgba(0,210,106,0); }
    50%      { transform: scale(1.02); box-shadow: 0 0 10px 3px rgba(0,210,106,0.18); }
  }
  @media (prefers-reduced-motion: reduce) {
    .paper-cta--primary,
    .paper-cta:not(.paper-cta--primary) { animation: none; }
  }

  @media (max-width: 767px) {
    .paper-cta:not(.paper-cta--primary) { display: none; }
  }
</style>
