# ============================================================================
# REQUIRED ENVIRONMENT VARIABLES
# ============================================================================
# These variables are REQUIRED for the application to function.
# For local development, copy this file to .env and fill in your values.
# For production, set these as GitHub Secrets (see AUTH_SECRETS.md).
# ============================================================================

# ----------------------------------------------------------------------------
# SUPABASE AUTHENTICATION (REQUIRED)
# ----------------------------------------------------------------------------
# Get these from your Supabase Project Settings > API:
# https://supabase.com/dashboard/project/_/settings/api
#
# SECURITY NOTE: The Anon Key is SAFE to expose in client-side code.
# It has the same permissions as an anonymous user (RLS policies apply).
# NEVER commit the Service Role Key - it bypasses all RLS!
# ----------------------------------------------------------------------------

# Your Supabase project URL (e.g., https://abcdefgh12345678.supabase.co)
PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Your Supabase public anon key (starts with 'eyJ...' or 'sb_publishable_')
PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ----------------------------------------------------------------------------
# APPLICATION MODE (OPTIONAL)
# ----------------------------------------------------------------------------
# Controls feature visibility for public vs development builds.
# 
# Values:
#   - 'public'  : Hides dashboard builder, returns 404 for /dashboard-builder
#                 Shows "Business only" badge on "New" button (for sales)
#   - 'dev'     : Full builder access for development
#
# Default: 'public' (safe default for production)
# ----------------------------------------------------------------------------
PUBLIC_MODE=public

# ----------------------------------------------------------------------------
# SITE CONFIGURATION (OPTIONAL)
# ----------------------------------------------------------------------------
# Used for email redirects and SEO. Defaults to window.location.origin if unset.
PUBLIC_SITE_URL=https://www.hackfluency.com

# ============================================================================
# GITHUB SECRETS SETUP INSTRUCTIONS
# ============================================================================
# 1. Go to: https://github.com/yourusername/special-palm-tree/settings/secrets
# 2. Click "New repository secret"
# 3. Add the following secrets (match the variable names above):
#
#    Name: PUBLIC_SUPABASE_URL
#    Value: https://yfofmawbvlontugygfcj.supabase.co
#
#    Name: PUBLIC_SUPABASE_ANON_KEY
#    Value: sb_publishable_EFI12D4AaO2Y7o5gyWrB-g_i--T5Arz
#
#    Name: PUBLIC_MODE
#    Value: public
#
# 4. The deploy workflow will automatically inject these into the build.
# ============================================================================
